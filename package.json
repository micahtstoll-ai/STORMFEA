{
  "name": "stormfea",
  "version": "43.0.0",
  "description": "STORMFEA — FDM-aware FEA for FTC · Nordic Storm Team 5962",
  "type": "module",
  "scripts": {
    "build": "tsc && node scripts/copy-client.mjs",
    "copy:client": "node scripts/copy-client.mjs",
    "test": "tsc && node dist/tests/solver_validation.js && node scripts/test_client_logic.mjs",
    "start": "node dist/index.js",
    "dev": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^2.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/multer": "^1.4.11",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
