# AI Orchestration Log - STORMFEA

This log tracks how AI orchestration tools (like Claude Code) are utilized to build, audit, and optimize the STORMFEA computational engine.

## Verification

Each entry records what is verifiable about the work: CI state, suite counts,
and the locking tests. Entries 5-11 were backfilled on 2026-08-03 from the git
history, the roadmap, and the audit documents rather than written live, so they
record only what the tree and commits can confirm.

## Orchestration Patterns In Use

Three working patterns emerged over June–August 2026; entries below name which
one they used.

1. **Audit → issues → phased plan → small PRs.** A read-only analysis pass
   files numbered findings, a companion plan sequences them by
   risk-reduction-per-effort with explicit dependencies, then each phase lands
   as an independently verifiable PR. Used for the July audit (#96–#111) and
   the layer-model audit (A1–A7). The plan document is the durable artifact —
   `docs/REPO_ANALYSIS_2026-07.md` + `docs/IMPLEMENTATION_PLAN_2026-07.md`.
2. **Wide campaign → single reviewed integration.** Many parallel agent
   sessions produce many branches; they are reviewed and squash-merged as ONE
   CI-green integration commit rather than 30 sequential merges to `main`.
   Used for the 69-issue solver-accuracy campaign (commit `42143a2`). The
   deferral discipline matters: #149 was cut from the integration because its
   mechanism did not work, and stayed open instead of shipping broken.
3. **Invariant-locked refactoring.** Before an AI session is allowed to touch
   the two-region or interlayer model, the numbered invariants in `CLAUDE.md`
   and their resolved index in `docs/INVARIANTS.md` define what must not
   change; the locking tests named there are the acceptance gate. This is what
   makes "flag off must stay bit-identical" enforceable across sessions that
   share no context.

## Engineering Log

### Entry 1: Vitest Framework Setup

* **Date:** 2026-06-25
* **Task:** Establish Vitest test framework with configuration and initial test structure
* **Verification:** Code-review verified
* **Engineering Notes:**
  * **Files Modified:** `vitest.config.ts` (new), `server/tests/unit/stiffness-matrix.test.ts` (new)
  * **Configuration Choices:**
    - Environment: Node.js (no browser, improves test speed for server-side solver logic)
    - Test File Pattern: `server/tests/**/*.test.ts` — scans entire test directory recursively
    - Coverage Thresholds: 70% minimum for lines, functions, branches, statements — ensures broad instrumentation without false positives from unreachable code paths
    - Path Alias: `@/` → `./server` — enables clean imports in tests without relative paths
  * **Coverage Report Configuration:** Excludes server tests themselves, entry point, and type definitions to avoid circular counting
  * **Initial Test Placeholder:** Created dummy test suite in `server/tests/unit/stiffness-matrix.test.ts` with 3 tests to validate framework connectivity
  * **Decision Rationale:** 
    - Chose v8 provider for native Node.js compatibility (no instrumentation library overhead)
    - Excluded main `server/index.ts` from coverage because it's an Express entry point with complex async bootstrapping — better tested via integration tests
    - 70% threshold chosen to catch obvious gaps (missing branches) without requiring coverage of error-handling paths in production code

---

### Entry 2: Gyroid Infill Tensor — Density-Based Constitutive Matrix Scaling

* **Date:** 2026-06-25
* **Task:** Implement GyroidOrthotropic material model with empirically-derived density scaling formulas
* **Verification:** 35+ test cases pass, matrix properties validated
* **Engineering Notes:**
  * **Files Modified:** 
    - `server/solver/types.ts` — Added `GyroidOrthotropic` interface and type guards
    - `server/solver/element.ts` — Added `buildGyroidConstitutiveMatrix()` function with power-law degradation
    - `server/tests/unit/density-matrix.test.ts` — Added comprehensive test suite (512+ lines, 35+ cases)
  
  * **Mathematical Foundation:**
    - Base Material: PLA (3D-printed thermoplastic) — solid modulus values: E_xy=3500 MPa, E_z=2275 MPa
    - Density Parameter: ρ ∈ [0, 1] — relative infill fraction (0=void, 1=solid)
    - Power-Law Degradation with Correction Factors:
      ```
      E_xy(ρ) = 3500 × ρ^1.75 × (1 - 0.12(1-ρ))
      E_z(ρ)  = 2275 × ρ^2.1  × (1 - 0.18(1-ρ))
      G_xz(ρ) = 1143 × ρ^2.3  × (1 - 0.22(1-ρ))
      ```
    - Power-law FORM is cited (Gibson & Ashby 1997; the pattern-effect direction is supported by Birosz et al. 2022, Hikmat et al. 2023). The specific exponents (1.75, 2.1, 2.3) are STORMFEA engineering estimates chosen WITHIN the cited open-cell ranges — no paper reports these pattern-specific coefficients. Confidence LOW; regression-locked (gyroid-formula.test.ts) and calibration-overridable
    - Linear correction factors [(1 - α(1-ρ))]: account for strut curvature and surface roughness effects near 0% and 100% density
  
  * **Implementation Architecture:**
    - `GyroidOrthotropic` interface: immutable, strongly-typed discriminator `kind: "gyroid-orthotropic"`
    - Type guards: `isGyroidOrthotropic()`, `isOrthotropicLike()` — enable compile-time exhaustiveness checking on material unions
    - Material dispatcher: `buildAnyConstitutiveMatrix()` routes GyroidOrthotropic to dedicated builder, other types to existing handlers
    - Constitutive matrix output: 6×6 symmetric positive-definite (SPD) Float64Array in Voigt notation — validates via Cholesky decomposition
  
  * **Validation & Testing:**
    - Test Suites: 20% density (114 MPa E_xy), 50% density (583 MPa), 100% density (3500 MPa = solid reference)
    - Invariants checked:
      1. Matrix symmetry: C[i,j] = C[j,i] within 1e-10 (numerical precision limit)
      2. Positive definiteness: All eigenvalues > 0 (verified via Cholesky decomposition success)
      3. Density scaling: E_xy(50%) / E_xy(20%) ≈ 5.17× (ratio implied by the chosen power-law exponent, not an independently measured value)
      4. Anisotropy preservation: E_z < E_xy (vertical direction softer than in-plane — artifact of FDM layering)
      5. Shear decoupling: C[0:3, 3:6] ≈ 0 (normal and shear blocks independent for transversely isotropic material)
    - Edge cases: Rejects density < 0, density > 1, invalid Poisson's ratio (ν ≥ 0.5)
  
  * **Decision Rationale:**
    - Chose power-law (not linear) model because gyroid lattice shows non-linear stiffness degradation — linear underestimates stiffness at high density, overestimates at low density
    - Different exponents for E_xy vs. E_z reflect that vertical struts (along Z) are longer and more compliant than horizontal struts — a modeling choice within the cited Gibson-Ashby ranges, not a fitted result
    - Constant Poisson's ratio (ν_xy=0.38, ν_xz=0.28) simplifies input (one fewer parameter) and is valid for most composite lattices over practical density range
    - Correction factors small (α ∈ [0.12, 0.22]) to preserve dominant power-law behavior while accounting for geometry effects near boundaries

---

### Entry 3: Canvas Changes for 3D Deformation Wireframe

* **Date:** 2026-06-25
* **Task:** Decode and store nodal displacements from analysis server response for deformation visualization
* **Verification:** integrated with displacement response decoding and validation
* **Engineering Notes:**
  * **Files Modified:** `client/index.html`
  
  * **Implementation:**
    - Added `S.displacements` global property to store per-vertex displacement field (Float32Array)
    - New function `decodeDisplacementB64()` — decodes base64-encoded binary displacement data from server
    - Integration points: `runAnalysis()` function — reads `dataStd.vertexDisplacementB64` and `dataFine.vertexDisplacementB64` from server response
  
  * **Data Format:**
    - Server encodes displacements as Float32Array → base64 string (one float per DOF per vertex)
    - Client decodes: `Uint8Array.from(atob(b64), c => c.charCodeAt(0))` → Float32Array (reinterpret bytes as 32-bit floats)
    - Expected length: `surfaceTriangleCount × 3 vertices × 3 DOF = surfaceTriangleCount × 9` floats
  
  * **Validation & Logging:**
    - Verifies decoded array length matches expected vertex count; logs warning on mismatch
    - Console logs: `[displacement] array size mismatch` or `Loaded displacement field: N vertices`
    - Handles null/undefined gracefully (some analysis modes may not return displacements)
  
  * **Integration with Analysis Pipeline:**
    - Invoked after standard stress color apply (applyStressColors)
    - Supports both standard and fine-mesh analysis branches
    - Preserves displacement data across re-analysis for comparison workflows
  
  * **Architectural Notes:**
    - Displacements stored separately from stresses — enables future decoupling of visualization modes (show stress only, displacement only, or both)
    - Float32Array chosen for memory efficiency: 4 bytes per float vs. 8 for Float64 — critical for large meshes (100k+ vertices)
    - Base64 encoding used by server for HTTP transport; client-side decode avoids redundant JSON parsing overhead

---

### Entry 4: G-Code Parser — Edge Cases & Slicer Detection

* **Date:** 2026-06-25
* **Task:** Implement G-Code parameter extraction with slicer detection and layer height inference fallback
* **Verification:** 18 test cases in test group E, all passing
* **Engineering Notes:**
  * **Files Modified:** `client/index.html` (UI, drag-drop zones), `scripts/test_client_logic.mjs` (test group E)
  
  * **Feature Overview:**
    - New G-Code drop zone (flex:2) alongside STL/STEP drop zone (flex:3)
    - Accepts `.gcode` files from 3D printer slicers
    - Extracts print parameters (layer height, layer count, extrusion width) automatically
    - Maps extracted layer height into Material tab UI slider (`s-lh`)
  
  * **Slicer-Specific Parameter Extraction:**
    1. **PrusaSlicer (2.x+)**
       - Detection marker: `; generated by PrusaSlicer`
       - Layer height: `;HEIGHT:X.XXX` (first line = first layer, subsequent = regular layers)
       - Layer count: `;LAYER_COUNT:N`
       - Extrusion width: `;WIDTH:X.XXX`
       - Test E1: Validates 5 parameters across 120 layers
    
    2. **BambuStudio (1.8.0+)**
       - Detection marker: `; generated by BambuStudio`
       - Layer height: `; layer_height = X.XX`
       - Initial layer: `; initial_layer_print_height = X.XX`
       - Line width: `; line_width = X.XX`
       - Test E2: 80 layer file with all four parameters
    
    3. **Cura (5.x+)**
       - Detection marker: `;Generated with Cura_SteamEngine` or `;FLAVOR:Marlin`
       - Layer count: `;LAYER_COUNT:N`
       - Layer height: Extracted via **Z-delta fallback** (see below)
       - Test E3: Layer height computed from G0 Z commands (delta = 0.28mm)
    
    4. **Unknown/Custom Slicers**
       - Falls back to **Z-delta inference** if slicer-specific markers not found
       - Scans G-code for `G0 Z<height>` commands, computes delta between first two Z positions
       - Handles edge cases: Z=0 excluded (homing), only considers positive moves
       - Test E4: Correctly infers 0.3mm layer height from Z moves
  
  * **Edge Cases Handled (from Test Group E):**
    - **E1 (PrusaSlicer):** Multiple HEIGHT comments (first layer different from regular) — uses MODE to distinguish
    - **E2 (BambuStudio):** Sparse comments (not every line) — regex scans entire file, captures first match per parameter
    - **E3 (Cura):** Missing slicer-native params — Z-delta fallback activates, computes 0.28mm from 10 consecutive G0 commands
    - **E4 (Unknown):** No recognizable markers — purely Z-delta, slicerDetected='unknown'
    - **E5 (Empty):** Zero-line input — returns null for all parameters, slicerDetected='unknown'
  
  * **Implementation Details:**
    - `inferLayerHeightFromZ(lines)` — utility function for Z-delta computation
      - Scans for `G0 Z<height>` pattern
      - Filters out Z=0 (homing), collects first 10 unique Z values
      - Computes median delta between consecutive Z positions
      - Returns null if fewer than 2 Z positions found
    
    - `parseGcodeParams(lines)` — main dispatcher
      - Takes line array (file split on newlines)
      - Returns object: `{ slicerDetected, layerCount, layerHeightMm, firstLayerHeightMm, extrusionWidthMm }`
      - All fields nullable; null indicates parameter not found in file
    
    - Chunked FileReader (4MB chunks) — supports large G-code files (typical slicer output: 1–10 MB for multi-hour prints)
    - Ephemeral toast feedback: `_showGcodeImportToast()` — confirms params extracted and applied
    - Event listener: `gcodeParseComplete` — wires to Material tab UI update
  
  * **Decision Rationale:**
    - Z-delta fallback (rather than hard error) improves UX — many slicers support Marlin but don't emit standard layer-height comments
    - Median instead of mean for layer-height delta — robust against spurious Z jumps (e.g., ooze prevention)
    - Chunked FileReader chosen over `blob.text()` for browsers with large-file limits
    - 4MB chunk size — empirically chosen to balance memory and I/O efficiency
    - Sparse UI validation — only warns if parameter extraction fails; allows continue-without-gcode (GCode is optional feature)

---

### Entry 5: Landing Screen, Brand Retheme, and the Client Audit Sweep

* **Date:** 2026-06-30 → 2026-07-01 (PRs #73–#93)
* **Task:** Give the app a real entry point, lock the visual identity, and clear the accumulated correctness debt in the single-file client
* **Verification:** Backfilled. Verified by `scripts/test_client_logic.mjs` growth and per-PR CI
* **Pattern:** Audit → issues → small PRs (client-scoped)
* **Engineering Notes:**
  * **Files Modified:** `client/index.html` (dominant), `scripts/test_client_logic.mjs`, `server/index.ts`
  * **Structural changes:**
    - Landing screen added as the app entry point; CALIBRATE and SOURCES removed as top-level tabs (they were competing with the SETUP → ORIENT → LOADS → MATERIAL → RESULTS spine)
    - Load-part and load-file collapsed into a single drop zone accepting STL, STEP, and G-Code — three entry paths had three different failure behaviors
    - Nav moved to the left rail, button subtitles dropped
  * **Retheme:** Unified the palette around the Nordic Storm brand mark; added the tier-1 rain canvas
  * **The rain canvas cost five follow-up commits** (`163a7e1`, `0e98570`, `4046f18`, `4ac6b5d`, `a96d3e6`, `70a4215`) — DPI scaling, viewport sizing, and four separate z-index corrections. **Lesson:** a full-viewport decorative canvas interacts with every absolutely-positioned overlay in the app. The fix that finally held was structural (wrap landing content in one `z-index:1` layer) rather than another per-element z-index bump. Prefer establishing one stacking context over patching siblings
  * **Audit sweep — three consecutive review passes on `client/index.html`:**
    - `6272db4` — 14 correctness and structural bugs
    - `5a77012` — 11 first-time-user workflow fixes
    - `d0d91a1` — 12 student-facing UX polish fixes
    - `18f2830` — convergence badge was claiming convergence when `safetyFactorAvailable` was false
  * **Two bugs worth remembering:**
    - `399eef0` — `expectedLength` in `applyStressColors` was wrong, so the heatmap silently did not render. The array-length invariant now documented in `CLAUDE.md` ("vertexStress.length === triangleCount * 3") comes from this class of failure
    - `7fbfadd` — unguarded `insertBefore` threw when the reference node had no parent
  * **Timeouts:** solver ceiling raised 120s → 600s (`21a2cf8`) once fine C3D10 meshes became the common case rather than the exception
  * **Decision rationale:** the client is a single file with no build step, which makes broad AI review passes cheap and effective (whole-file context fits) but makes regressions invisible without tests — hence every sweep landed alongside additions to the client-logic checks

---

### Entry 6: July Repo Audit — Analysis, Phased Plan, and Correctness Execution (#96–#111)

* **Date:** 2026-07-07 → 2026-07-08 (PRs #94, #95, #112, #113)
* **Task:** Full-repo audit, then execute the resulting issues in dependency order
* **Verification:** Backfilled. Verified by the solver validation suite plus, from this batch onward, CI running the real TetGen/Gmsh binaries
* **Pattern:** Audit → issues → phased plan → small PRs
* **Engineering Notes:**
  * **Artifacts produced:** `docs/REPO_ANALYSIS_2026-07.md` (findings with test evidence) and `docs/IMPLEMENTATION_PLAN_2026-07.md` (phases, dependencies, exit criteria per phase)
  * **Phase 0 was guardrails, deliberately before any solver edit** (#108, `6d0622e`) — CI now `apt-get install`s tetgen and gmsh and runs `scripts/verify_tetgen_c3d10.mjs` plus one STL and one STEP end-to-end analysis. Every later phase modified the solver hot path; this is the net. A wall-time gate followed (`4356cbb`) reporting actual-vs-budget with an env override
  * **Phase 1 — correctness of numbers users already see:**
    - #96 (`83c3db7`) — SPR smoothing and the ZZ estimator used a hardcoded 4-node element stride, corrupting every quadratic-mesh heatmap and error estimate. Three stride fixes plus a linear-field exactness test
    - #97 (`6190811`) — the verdict and bulk SF ignored the criterion's `minSafetyFactor` and ran off analytic checks instead
    - #105 (`7d11e8a`) — STL bolt constraints were unbounded axially, over-constraining ~28% of nodes; bounded with the 3-D cylinder test. SF drops on bolted parts, and that is the fix working
    - #101 (`773c183`) — the upright orientation swap dropped inter-layer `G_xy`
    - #99 (`9bb64ec`) — modal mass used label matching instead of `massRho`, ignoring infill entirely
    - #102 (`db55181`) — `nu_xz` was not following the standard engineering convention in the orthotropic compliance build
    - #67225a6 — sign error in the C3D4 `dN/dy` cofactors
  * **Phase 2 — performance:**
    - #104 (`2543d2b`) — error mapping was O(V x nodes x elements); replaced with node→element adjacency
    - #100 (`bd14797`) — K, the sparsity pattern, and the IC(0) factorization are reused across solves in a session
    - #98 (`c7d24b9`, later `ab581ae`) — `worker_threads` parallel assembly was dead under ESM; revived, then given transferable CSR slabs and a persistent worker pool. **This is why `test-parallel-assembly.ts` exists as an equivalence suite** — parallel assembly must reproduce serial bit-for-bit, and the two-region material field later had to cross that same postMessage boundary (two-region invariant 7)
  * **Server hardening:** POST bodies validated before heavy work with a uniform error envelope (#106, `4564b4f`); atomic user-data writes (#111, `33f4783`); TetGen reports a missing binary honestly instead of retrying pointlessly (`ba14863`); Three.js and fonts vendored for true offline use (#107, `234f30e`)
  * **Live progress:** real phase progress and cancellation over SSE (#109, `d76a4b5`), then a live CG residual sparkline (`d8af209`)
  * **Decision rationale:** the phase ordering (guardrails → correctness → performance → research) is the reusable part. Performance work on an unverified solver optimizes the wrong answer faster; the #96 stride bug would have been baked into every subsequent benchmark

---

### Entry 7: Solver Capability Expansion — Body Loads, Pressure, Buckling on Quadratic Meshes, Modal Visualization

* **Date:** 2026-07-09 → 2026-07-11 (PRs #114–#122)
* **Task:** Close the gap between "what the solver computes" and "what a student can actually ask it"
* **Verification:** Backfilled. Verified by new benchmark groups in `solver_validation.ts` and per-feature self-review pass (`1ae0f24`)
* **Engineering Notes:**
  * **Files Modified:** `server/solver/{buckling,load,mass,modal}.ts`, `server/analysis.ts`, `client/index.html`
  * **New physics capability:**
    - Self-weight / gravity body load plus STL mesh-quality control (`05c0366`)
    - Surface pressure loads with orthotropic and pressure benchmarks (`a5fd2c9`), then a selectable load region — face / facing / all (#8, `29b2faa`) and suction (negative) pressure
    - C3D10 geometric stiffness (`cc2abc0`) — buckling now runs on the DEFAULT quadratic mesh instead of requiring a linear fallback
    - Fatigue gained a load-ratio R input rather than assuming R=0 (#4, `2072935`)
    - Per-DOF (roller) BCs, which also corrected the coupon noise-floor record (#7, `c38203c`)
    - Upright orientation moved from a scalar swap to the exact Bond transform (#1, `a11062b`)
  * **New visualization:** deflected-shape view with modal frequencies and mode-shape animation (`787b1be`); section / cutting-plane view for internal stress (`b4f2f1c`), later capped with a solid face via the stencil buffer (`a394094`) because an uncapped cut reads as a hollow part
  * **Known-limitations campaign:** two commits (`9c2f314`, `f2ba0ef`) specifically targeted limitations that were code-bound rather than data-bound — fallback C3D10, normal pressure, the Kt benchmark, hole-merge warning, TetGen documentation, gyroid sourcing. **Distinguishing "we can fix this by writing code" from "this needs coupons on a printer" is the single most useful triage question for this project**; the remainder are the honest entries in the roadmap's KNOWN LIMITATIONS
  * **Session persistence** had to follow the features (`0535d24`) — new solver options and body/pressure loads were not surviving resume
  * **Self-review caught three defects before merge** (`1ae0f24`): crease normals, pressure sign, gravity validation. Worth noting as evidence that a dedicated review pass on the agent's own diff pays for itself

---

### Entry 8: Two-Region Material Model — Phases A–F plus Gibson-Ashby Core Homogenization

* **Date:** 2026-07-11 → 2026-07-13 (PRs #122, #123, #128)
* **Task:** Replace the single averaged material with a geometric shell/core split — dense perimeter walls vs homogenized infill core
* **Verification:** Backfilled. Verified by `solver_validation.ts` group 25 (bit-identity to 1e-12 with the flag off), the sandwich-cantilever composite-EI anchor, and a Taguchi L9 sweep
* **Pattern:** Invariant-locked, staged phases
* **Engineering Notes:**
  * **Files Modified:** `server/twoRegion.ts` (new), `server/solver/{distance,wallfrac,lattice,types}.ts`, `server/analysis.ts` (`buildCoreMaterial`), `client/index.html`
  * **Phase sequence — each phase shipped independently verifiable:**
    - A (`14c5317`) — per-element material field infrastructure in the solver, no behavior change
    - B+C (`bda0633`) — shell/core classification and material construction
    - D (`9a1503a`) — region-aware downstream yields
    - E (`94bea89`) — the opt-in flag, MATERIAL-tab toggle, summary surfacing
    - F (`3654512`) — validation: solve equivalence, sandwich beam, anchor policy
    - Then the Taguchi L9 orthogonal-array sweep and documentation (`81da38b`)
  * **Result that justifies the model:** the sandwich cantilever matches composite-EI theory to 0.3%, where the homogenized single-material model is ~23% too soft. That is the headline number for why a geometric split beats an averaged one
  * **Classification is geometric, not heuristic:** exact point-to-triangle surface-distance field (`distance.ts`) plus marching-tet volume fractions into 9 Voigt-blended bins. **Nearest-NODE distance was tried and rejected** — boundary triangles are 3–6 mm against a ~1.35 mm wall band, so node distance aliases the band away entirely (now two-region invariant 4)
  * **Gibson-Ashby core homogenization, two stages:**
    - Stage 1 (`1d434b2`) — the core scales the SOLID material by per-pattern-family power laws in `solver/lattice.ts` (TPMS-like ρ^1.75 stiffness / ρ^1.25 strength; extruded-wall ρ^2.0 / ρ^1.5; lightning x0.3) instead of scaling linearly in density. Orientation was removed from core stiffness here; 0% infill stopped crashing via a 1e-3 floor
    - Stage 2 (`1416733`) — per-axis laws in the natural material frame, and the bins became TRUE Voigt blends of the two ROTATED endpoint C matrices. Anisotropy INVERTS at low density for extruded-wall patterns (ρ^1.0 along build axis, ρ^2 in plane, ρ^3 in in-plane shear), which a symmetric Poisson guard keeps positive definite in every bin
  * **The invariant that makes this safe to hand to a future agent session:** blending after the Bond rotation is exact only because rotation is linear in C's entries, and only while shell and core share a `weakAxis`. Blending engineering constants instead is equivalent ONLY when the two share every modulus ratio — which the anisotropic core laws deliberately break. Written up as two-region invariant 3 precisely because it is the mistake a plausible-looking refactor would reintroduce
  * **Endpoint anchoring over renormalization:** 100% infill and all-shell parts collapse to the uniform path exactly (`materialsEqual` depends on the ρ=1 scale factors being exactly 1.0). Interior divergence from the legacy scalar multiplier is REPORTED in `summary.materialModel`, never hidden by rescaling
  * **UI surfacing** (`4a9c612`) — live wall-band readout (wall count x line width) and the shell/core vs legacy strength divergence with relative delta. Then floors and ceilings modeled as independent top/bottom solid skins (`d086f30`)

---

### Entry 9: Layer-Model Audit A1–A7 — Dual Criterion, Bond Model, Delamination Surfacing

* **Date:** 2026-07-13 → 2026-07-18 (PRs #127, #132–#135)
* **Task:** Audit how the tool accounts for FDM layers, then resolve every defect found
* **Verification:** Backfilled. Verified by `fdm-criterion.test.ts`, `bond.test.ts`, `in-plane-anisotropy.test.ts`, and the azimuth-invariance group in `solver_validation.ts`
* **Pattern:** Audit → issues → phased plan; artifact is `docs/layer-model-audit.md`
* **Engineering Notes:**
  * **Files Modified:** `server/solver/stress.ts` (`fdmDualCriterionSF`), `server/solver/bond.ts` (new), `server/analysis.ts`, `server/report.ts`, `client/index.html`
  * **A1 — the finding that forced the rewrite:** the "transversely isotropic" Hill form was azimuth-DEPENDENT in the layer plane. Rotational symmetry requires `N = F + 2H`, but the implementation set `N = 3/(2Y²)` independently, which only holds when `Z = Y`. At `Z = 0.58Y` the identical physical pure-shear state gave a **1.7x safety-factor swing depending on the part's rotation on the build plate**. No coefficient tuning fixes this — a single quadratic form cannot satisfy in-plane isotropy, uniaxial yield Y, in-plane shear Y/√3, and through-thickness Z ≠ Y simultaneously
  * **Fix (`f208bfe`):** split into `fdmDualCriterionSF` — bulk von Mises (a norm, azimuth-invariant by construction) MIN a separate interface check. Anchors preserved rather than re-derived: in-plane uniaxial at Y, through-layer at Z, interlayer shear at S_zs, and the flat-print false-safety SF = Z/Y ≈ 0.58 which is the tool's core claim. Default `S_zs = yieldZ/√3` is EXACTLY Hill's transverse shear, so uncalibrated results match the legacy criterion
  * **A3 (`f208bfe`):** the interface was tension/compression symmetric. Now a Macaulay bracket on ⟨σzz⟩₊, with compression routed to bulk von Mises and credited interlayer shear via Mohr-Coulomb (μ=0.3, LOW confidence)
  * **A4 (`7462968`):** the orientation multiplier double-counted the layer penalty — orientation was in both the material scalars and the criterion. `materialStrengthMultiplier` is now orientation-free; the only orientation scalar left in the material path is `angledNoBedFallbackMul` (0.75), which survives because no directional model exists for that case
  * **A5:** lap-shear calibration could not disagree with the Hill coupling. `yieldZShear` (lap-shear coupon) and `yieldZ` (Z-tension coupon) are now independent inputs; the `τ/0.58` conversion survives only as a flagged no-Z-coupon fallback
  * **A6 (`d3e29b2`) — the bond model,** `server/solver/bond.ts`: lumped-capacitance cooling → Frenkel/Pokluda neck growth → reptation healing. **The design decision that makes it safe: it is RELATIVE, not absolute.** Multipliers are exactly 1.0 at the reference condition (per-material nozzle ref, 60 mm/s, fan 100%, bed 60 °C) evaluated at the SAME layer height, so no process block means a bit-identical legacy path and the layer-height slope stays owned by `layerHeightFactor`. Constants are LOW confidence, regression-locked, overridable via `CalibrationProfile.bondCoeffs`, fittable through `POST /api/calibration/bond-sweep`. **Trend locks over value locks** — hotter nozzle up, more fan down, faster printing up; flipping a trend requires physical evidence, not a refactor
  * **A7 (`19df2c5`):** in-plane raster (bead-to-bead) anisotropy, opt-in and evidence-gated, applied as a separate `min` on the BULK term only so A1's interface azimuth invariance is untouched. Bit-identical when off or when no evidence is supplied
  * **Downstream surfacing** — the model is only useful if it reaches the user: layer-by-layer interlayer risk profile (`9ca064f`), delamination-specific DFM guidance (`745d4bb`), coupon recommendation engine that names WHICH coupon most raises confidence (`a24c90e`), process-sensitivity dashboard with a nozzle x speed sweet-spot map (`0a94b65`), void/consolidation factor for cold deposition (`1819ccf`), wall-to-wall bead bonding as a distinct failure mode (`3c64f2b`), and report disclosure of criterion, bands, and material model (`30dacfc`)
  * **`hill-legacy` stays callable** — `AnalysisSettings.criterion` and the upright-no-bed scalar-swap fallback both depend on it, because the interface criterion needs a known weak axis and that swap deliberately has none
  * **Documentation:** `1e2185e` synced all docs to the solver state; the audit doc is kept in present tense as a historical derivation record with a resolution table, which turned out to be the right call — it explains WHY the invariants exist to a session that has no memory of the audit

---

### Entry 10: Solver-Accuracy Campaign — 69 Issues, 30 PRs, One Integration

* **Date:** 2026-07-30 (integration commit `42143a2`; issues #136–#205 minus #149)
* **Task:** Audit the solver end to end and land the result as one reviewed, CI-green integration
* **Verification:** Backfilled. Verified at merge: 677 vitest tests, solver validation 180/0, parallel-assembly equivalence, client logic 141/0, plus three new CI drift gates
* **Pattern:** Wide campaign → single reviewed integration
* **Engineering Notes:**
  * **Headline defect (#167):** Gmsh's C3D10 midside node ordering was SWAPPED, making every STEP-file element self-intersecting — mixed-sign Jacobian, ~0 mm³ isoparametric volume. Post-fix the isoparametric volume matches CAD exactly. **Both mesher paths now run a runtime midside self-check rather than trusting the binary** (`server/c3d10_ordering.ts`). The June re-verification of `C3D10_REORDER` for TetGen (`dbb230c`, `970ae4b`) had established the empirical-verification habit; this is the same class of bug in the other mesher, and the reason the check is now permanent rather than a one-time script
  * **Grouped outcomes** (full itemization lives in ROADMAP.md under "Solver-accuracy campaign"):
    - *Element formulation* — C3D10 surface traction was lumping A/3 onto corners with midsides getting zero, exactly inverted from the correct quadratic consistent load (#137); C3D10 mass now integrates isoparametrically instead of assuming affine (#158); geometric stiffness keeps the linear stress gradient that drives bending buckling (#164); tangled elements are detected rather than integrated through |detJ| (#162)
    - *BCs and hygiene* — support reactions are recovered from pristine pre-BC rows (they had collapsed to ~0 through the penalty-modified K) (#136); exact `elimination` replaces a single global-max penalty that was degrading CG conditioning (#154, #155); CG re-checks the recurrence residual against the true residual and the iteration cap is no longer warn-only (#153)
    - *Eigenproblems* — buckling moved to block subspace (Rayleigh-Ritz) inverse iteration, so the smallest positive BLF is GUARANTEED rather than hoped for (#138); modal gained a Sturm missed-mode check (#160)
    - *Error estimation* — the ZZ estimator became a real volume-weighted energy norm over the full stress tensor; it had been an unweighted L2 norm of scalar von Mises differences with the material factor cancelling out (#143–#145), now locked by a manufactured-solution effectivity index (#150). Richardson reports observed order p_obs instead of hardcoding p=2 (#146)
    - *Scale invariance* — a recurring root cause: mesh quality judged a raw mm³ triple product against an absolute threshold (#165), singularity detection used a hardcoded 1 mm neighborhood (#148), TetGen baked in millimetre assumptions (#168), Gmsh face and hole detection were not relative to part scale (#169, #170). **Any absolute length or volume constant in geometry code is a latent bug** — that is the transferable lesson
    - *Material model* — three conflicting infill→stiffness laws differing 2–5x at 20% infill were unified (#176); core strength knockdown became per-axis like core stiffness, so the model can no longer claim Z-stiffer and Z-weaker at once (#177); Deshpande-Fleck-Ashby pressure-dependent core yield with α(1) = 0 exactly, so solid parts stay bit-identical (#171); bearing and thread strip-out on wall-lined holes use SHELL allowables, since slicers line holes with perimeters (#175)
    - *Honesty fixes* — the SF uncertainty band now WIDENS for Gibson-Ashby exponent uncertainty and LOW-confidence bond constants instead of being falsely tight exactly where the model is weakest (#172, #173); calibration fits gate on residuals so a bad sweep cannot lift confidence LOW→MEDIUM (#179); the Kt fixtures became real stress concentrators instead of hole-less boxes that made the "peak-based" correction a no-op (#139, #140); one shared `ACCEPTABLE_SF_THRESHOLD = 1.5` ended a three-way disagreement between verdict, caption, and report (#141)
  * **Three CI drift gates added** — `check-doc-test-counts.mjs`, `check-api-routes.mjs`, `check-invariants-symbols.mjs`. Every user-facing test count is asserted against the suite as it actually ran, every live route must appear in `docs/API.md`, and every invariant must resolve to a real symbol. **This is the durable orchestration lesson: documentation an agent can silently drift from needs a machine check, or the next session inherits confident wrong numbers**
  * **`docs/INVARIANTS.md` (#192)** — the resolved index from each CLAUDE.md invariant to its implementing symbol and locking test, including an honest list of five partial-coverage gaps rather than claiming full coverage
  * **The deferral (#149)** was cut from the integration because its TetGen regional sizing COARSENED instead of refining. Shipping it would have meant an "adaptive refinement" feature that made meshes worse. See Entry 11

---

### Entry 11: Closing the Adaptive-Refinement Loop (#149) and the TetGen Sizing Defect

* **Date:** 2026-07-29 → 2026-08-03 (PR #246, commits `75e1128`, `dba9e3f`)
* **Task:** Turn the ZZ error estimator's per-element indicator — computed "for refinement guidance" and driving nothing — into an error-driven targeted remesh
* **Verification:** Backfilled. Verified at merge: 706 passed / 8 skipped (714 total) across 69 files, solver validation 180/0, parallel-assembly equivalence, client logic 141/0, doc-count gate green
* **Engineering Notes:**
  * **Files Modified:** `server/solver/adaptiveMesh.ts` (new), `server/tetgen.ts` (`meshWithTetGenSizing`), `server/analysis.ts` (`runAdaptiveAnalysis`), `server/index.ts`
  * **Binary-independent core, deliberately separated:** `buildSizeField` maps per-element error η to per-node target edge length via the equidistribution law `h_new = h·clamp((η_target/η_e)^(1/p), min, max)`. High-error nodes shrink, low-error nodes stay coarse. Singularity regions are EXCLUDED — refining toward a true singular corner chases a quantity that diverges (the #147 lesson, now enforced in code). Plus `predictRefinedElementCount` / `relaxSizeFieldToBudget` as a runaway guard and `shouldStopRefinement` for loop control
  * **Opt-in with a bit-identity guarantee:** `analysis.adaptiveRefinement` defaults false ⇒ single tier solve, bit-identical to before. Only two inert seams were added to `runAnalysis` (`_prebuiltMesh`, the `_captureInternals` side-write), both undefined on the normal path — and `adaptive-default-identical.test.ts` asserts it rather than trusting the reading
  * **The follow-up fix is the real content of this entry.** Two CI failures in `adaptive-remesh.test.ts` were REAL DEFECTS, not flaky assertions: asked to refine a 4 mm cube's corner to ~0.67 mm, TetGen returned 12 elements — coarser than the 22-element base mesh. The size field was driving nothing. Two TetGen behaviors, both verified empirically against tetgen 1.5.0 (the version CI installs):
    1. **`-Y` defeats the sizing function.** Preserving the input surface triangulation forbids Steiner points on facets and segments, so a coarse boundary cannot be subdivided and near-surface sizes are unreachable. Removing `-Y` takes the same case from 12 to 314 elements
    2. **The metric is only read alongside `-q`.** `-pm` alone silently ignores the `.b.mtr` and emits the minimal tetrahedralisation (6 elements for a cube) — which made the old `-pmYQ` "sizing only" fallback a no-op
  * **Switch chain is now** `-pmq1.4Q` → `-pmq2.0Q` (both sized; the second only relaxes the radius-edge bound) → `-pq1.4Q` → `-pQ`, so the size field is abandoned only after two genuine attempts
  * **Dropping `-Y` does not cost the O(1) surface→volume map** — TetGen emits input vertices as the first N output nodes in input order either way, the property `meshWithTetGen` already relies on. Locked by a new test asserting each input triangle corner round-trips through `surfaceFaces` to a node with the submitted coordinates
  * **Two orchestration lessons:**
    1. **A failing test against a third-party binary is evidence, not noise.** The first instinct — that `adaptive-remesh.test.ts` was flaky — would have shipped a feature that silently did nothing. The switches were resolved by running tetgen 1.5.0 and reading element counts, not by reasoning about the documentation
    2. **The July deferral was correct.** #149 was held out of the 69-issue integration specifically because its sizing mechanism did not work. Keeping it open cost one follow-up PR; shipping it would have put a non-functioning "adaptive refinement" in front of users with a green suite behind it
  * **Roadmap drift to note:** `ROADMAP.md` still lists #149 under IN PROGRESS / NEXT as "the campaign's one deferral" — it was written on 2026-08-03 just before this work merged. The roadmap needs a COMPLETED entry for adaptive refinement

---

### Entry 12: The Adaptive Budget Overshoot, and Why the Sliver Fix Was Not Where Anyone Looked

* **Date:** 2026-08-03 (branch `claude/adaptive-mesh-refinement-budget-6nzpzs`, commits `d4483b5`, `6c8dbd2`)
* **Task:** A field report on the #149 loop: on a Ø5-bore tube the first refinement jumped 13,340 → 115,544 elements (8.7x against a documented 8x cap) and produced 13 slivers the hard mesh-quality gate (#166) rejected, so no refined solve ever completed. Three defects were named in the report; a fourth turned out to be the one that mattered
* **Verification:** 742 passed / 8 skipped (750 total) across 71 files, solver validation 180/0, parallel-assembly equivalence, client logic 141/0, all three CI drift gates green
* **Pattern:** None of the three cleanly — closest to invariant-locked refactoring, but the acceptance gate had to be BUILT before it could gate anything, because the benchmark it needed was impossible while no refined solve completed
* **Engineering Notes:**
  * **Files Modified:** `server/solver/adaptiveMesh.ts`, `server/tetgen.ts` (`meshWithTetGenSizing`, rewritten), `server/analysis.ts` (`runAdaptiveAnalysis`), `server/tests/unit/adaptive-mesh.test.ts`, `adaptive-remesh.test.ts`, `adaptive-benchmark.test.ts` (new), `README.md`, `docs/API.md`, `ROADMAP.md`
  * **The three reported defects were all real and are all fixed:** the growth budget was a prediction nobody ever compared against the mesh TetGen actually emitted; the cap was consulted by `shouldStopRefinement` one iteration late, so an over-budget mesh cost a full solve before anything noticed; and `minSizeFactor: 0.35` is a ~23x local density step in one jump (now 0.55, with a Lipschitz gradation limit on the size field and `maxIterations` 4 → 5)
  * **But the reported prime suspect for the slivers was wrong, and so was the first hypothesis after it.** Two measurements settled it. A uniform mesh at HIGHER density on the same part has zero hard-gate violations. And a perfectly CONSTANT metric — no spatial variation at all — pushed through `-m` still slivers: 5 / 33 / 56 violations at 18k / 68k / 121k elements, all on the curved outer wall, worsening with density. The size field's gradient was never the cause. **`-m` itself is.** Dihedral bounds (`-q1.4/10`), optimisation level (`-O10/7`), a tighter radius-edge ratio, and a global `-a` cap alongside `-m` each moved the count by a few and fixed nothing
  * **The re-mesh therefore moved off `-m` entirely onto `-r` with per-element `.vol` volume constraints** — zero hard violations on the same geometry and field. The structural reason is also the better argument: `-pm` re-tetrahedralises the raw PLC (here 32 tall quad facets) and must re-derive a fine boundary triangulation under a sizing function, while `-r` starts from the tier mesh's boundary — already fine and well-shaped, produced by the `-pq1.4a` path that measurably does not sliver — and only subdivides it. **Refining the mesh you have is what adaptive refinement means; re-meshing from scratch each iteration was never the intent**
  * **This supersedes Entry 11's switch chain.** The `-pmq1.4Q` → `-pmq2.0Q` → `-pq1.4Q` → `-pQ` chain recorded there is gone, replaced by `-rq1.4aQ` → `-rq2.0aQ` → `-rq1.4Q`. Entry 11's two TetGen findings were correct as far as they went — they explain why the metric was being READ at all — but they diagnosed a mechanism that should not have been used. The `-Y` finding still holds and still governs
  * **A wrong hypothesis that cost real time, recorded because it looks right:** the background mesh was being written with C3D10 midside nodes included, and those nodes never receive a target size, so 85% of the `.b.mtr` file was one placeholder constant (0.4375 mm) against a real field spanning 0.149–0.447 mm. That is a genuine defect and it is fixed (`extractCornerBackground`). It changed the output by exactly nothing — TetGen ignores unreferenced background vertices, and the emitted mesh was byte-identical at 80,580 elements. **A defect that is real, obviously bad, and causally irrelevant is the most expensive kind to find, and the only way to tell was to fix it and re-measure**
  * **Two calibration bugs surfaced from fixing the first three:** `predictRefinedElementCount` mixed a MEAN current size with a MIN target, so a field requesting NO refinement still predicted more elements than the mesh had — a floor that let the budget bisection bottom out over budget and spin the retries on an identical field. And the retry calibration must measure element GROWTH, not totals: a totals-based bias of 1.75 after the first refinement drove the next iteration's effective budget BELOW the current element count, which reads as "no refinement is possible" and stalled the loop a step early
  * **The acceptance gate now exists** (`adaptive-benchmark.test.ts`): adaptive reaches 0.262 global error on 40,534 elements where a uniform mesh of 54,373 reaches 0.337. It also locks what adaptivity does NOT buy — the two runs disagree on peak von Mises by 24% against an 8% spread in the error being optimised, so a lower energy-norm error is not a settled safety factor
  * **What the fix exposed, which is now the next constraint:** validated on a second geometry (a 40x20x4 mm bracket plate) the `-r` fix holds — clean 51,743-element mesh, zero hard violations, worst normalized Jacobian 0.112. But the PCG solver hit its 90 s deadline on the resulting 239k DOF system while still converging, and the run degraded to the tier solve. **The element budget and the solver's time budget are set independently and can contradict each other: the loop is allowed to build a mesh it is not allowed to solve.** Logged in ROADMAP
  * **Three orchestration lessons:**
    1. **A bug report's causal claim is a hypothesis, not a finding — even when its measurements are all correct.** Every number in the report was right. The 8.7x overshoot was arithmetic, the slivers were real, `minSizeFactor: 0.35` really is a 23x density step. The inference from the last of those to the slivers was wrong, and fixing all three named defects left 20 slivers where there had been 13. Separate what was measured from what was concluded
    2. **The decisive experiment is usually the one that removes the suspected cause entirely.** Not "tune the size field gentler" but "send a CONSTANT field and see if it still slivers". That took one run and ended the search; the tuning attempts before it took several and proved nothing
    3. **Fixing a gate reveals the next gate.** No refined solve had ever completed, so the solver wall-clock limit was invisible. Expect the first successful run of a previously-broken path to surface a new limit, and budget for it rather than treating it as a regression

---

### Entry 13: Two Risks Flagged Against the Adaptive Benchmark, Both Settled by Measuring Them

* **Date:** 2026-08-03 (branch `claude/stormfea-adaptive-benchmark-followup-r6xeqg`, follow-up to Entry 12)
* **Task:** Two concerns raised as PR #252 merged: (1) `adaptive-benchmark.test.ts` costs CI wall time — should it be gated behind an env var or a separate workflow? (2) the benchmark's uniform yardstick comes off a fixed TetGen volume-cap ladder, and TetGen's element count is non-monotone in `-a`, so a future TetGen could move the ladder and fail the gate for a reason that is not about the solver. Instruction was to measure both before changing anything, and to say "no action needed" if that is the answer
* **Verification:** post-merge CI on `main` (run 30848266810, sha `cd40fa3`) is GREEN, and the benchmark reproduces bit-identically on CI and on a 4-core container — 13,340 → 40,534 elements, ZZ error 0.8936 → 0.2624, uniform 54,373 elements at 0.3371, peak 4.905 vs 3.966 MPa. Suite unchanged at 750 vitest tests across 71 files, solver validation 180, client logic 141, all three drift gates green
* **Pattern:** Invariant-locked refactoring — the acceptance gate's load-bearing assertion is byte-identical, and everything added sits upstream of it as a premise
* **Engineering Notes:**
  * **Files Modified:** `server/tests/unit/adaptive-benchmark.test.ts`, `ROADMAP.md`, `docs/AI_ORCHESTRATION.md`. No production code
  * **(1) CI wall time: no action, and the per-file numbers are why.** The delta is +86 s on `main` (the "Compile TypeScript and run solver validation suite" step went 7m45s → 9m11s), all of it inside the vitest phase (210.18 s → 299.15 s). But vitest runs files in parallel, and the attribution is the interesting part: `adaptive-benchmark.test.ts` costs 271.8 s of CPU against a summed 788.1 s across 71 files — and `modal-solver.test.ts` costs 273.8 s. **The benchmark is not the critical path; it runs alongside a file that is slightly longer than it.** Gating it out would not return 272 s, it would return contention on a 4-core runner — the wall time was 210 s when the summed work was 302 s, so the box is bandwidth-bound, not latency-bound. For scale: the `apt-get install` step on the immediately preceding `main` run took 21m30s against 19 s on this one. **Runner variance is an order of magnitude larger than the thing being optimised.** An env-var gate would also mean the acceptance gate stops running on the PRs that are the only place it can catch anything
  * **A number that deserves attention more than the benchmark does:** `adaptive-resolve-failure.test.ts` went 30.2 s → 172.0 s across the same two runs. That is #252 working as intended (refined solves are attempted now, so the resolve-failure path has real solves to fail on) rather than a defect, but if CI time is ever actually cut, `modal-solver` at 273.8 s and this file are the targets, not the acceptance gate
  * **(2) The drift risk is real, untriggered, and was fixed at the failure mode rather than at the assertion.** CI's TetGen 1.5.0 reproduces the local ladder exactly, so nothing has drifted yet. The residual risk is that a future build shifts the rungs so the qualifying uniform mesh is far larger than the adaptive one — at which point the uniform error falls for reasons that have nothing to do with where elements were spent, the error-margin gate fails, and the failure reads as an adaptivity regression. **The margin assertion was left exactly as it was; a gate that is relaxed to survive drift is not a gate.** What changed is that the comparison's premise is now explicit: `UNIFORM_MAX_RATIO = 2.0` (measured ratio 1.34), checked in its own test, with the rungs TetGen actually produced carried in the failure text
  * **Out-of-band means the uniform solve is SKIPPED, not run.** Solving a mesh that is 3x the adaptive one would burn minutes to produce a number nobody should read; worse, on a slow runner it could push the 900 s `beforeAll` budget and turn a diagnosable premise failure into an undiagnosable timeout. The comparison tests then fail through one `comparison()` helper whose message names the premise, so no test ever dereferences a null result or reports a bogus solver number
  * **Verified by forcing it, not by reading it.** Temporarily setting `UNIFORM_MAX_RATIO = 1.1` made the real ladder out-of-band: the four regression assertions (refined solve completed, budget respected, converged, error reduced against its own start) still PASSED, and the four comparison assertions failed with `smallest qualifying uniform mesh has 54373 elements, 1.34x the adaptive 40534 ... TetGen produced [-a0.06 -> 20291, -a0.03 -> 54373, -a0.015 -> 79710, -a0.008 -> 126429]`. **A meshing-side change no longer takes down the half of the file that has nothing to do with meshing.** The forced-failure run also finished FASTER than the passing run (148 s vs 205 s) — three extra meshing calls cost less than the uniform solve they replaced, so the degraded path is not a wall-time risk either
  * **A dead branch found while reading the ladder:** it claimed in a comment to take the SMALLEST qualifying mesh, but `if (best !== null && n >= target) break` fired on the first rung that qualified, so the `n < best.mesh.elementCount` minimum-selection could never execute. Behaviour is now what the comment always said — and the early exit is kept, conditioned on the fairness band, so the passing path still makes exactly the two meshing calls it made before. Only a drifted ladder pays for the extra rungs, and that is precisely when walking them is worth it, because a FINER cap can yield a SMALLER mesh
  * **Two orchestration lessons:**
    1. **"Is X too slow?" is answered by attribution, not by totals.** 272 s sounds decisive until you notice it runs in the shadow of a 274 s file on a box that was already CPU-bound. The estimate this replaced (~4 minutes, from per-file timings on a slow container) was pessimistic by 3x for exactly that reason — summing serial per-file times models a machine nobody runs on
    2. **When a test is at risk of failing for the wrong reason, make the wrong reason state its name.** The tempting fix is to widen the assertion until drift cannot trip it, which trades a confusing failure for a gate that no longer gates. Splitting the premise from the claim keeps the claim sharp and moves the diagnosis to where the fix is

---

### Entry 14: The Heatmap Was Never Showing Its Colormap — a Color-Space Bug the Legend Test Could Not See

* **Date:** 2026-08-07 (branch `claude/heatmap-appearance-so9h1l`, PR #293)
* **Task:** A screenshot of the results view with the note "why does our heatmap look like ass." No error, no failing test, no reproduction steps — a rendering complaint against a suite that was fully green. Diagnose, then fix
* **Verification:** `npm run test` exits 0 — 976 vitest tests across 91 files, 187 solver validation tests, the parallel-assembly equivalence check, 182 client logic checks (159 → 182), and all four drift guards green including the #198 count guard against the updated README/CONTRIBUTING numbers
* **Pattern:** Invariant-locked refactoring. `CLAUDE.md`'s heatmap section defined what must not change (Gouraud shading, welding before color assignment, every vertex receiving a value); the new test group [T] in `scripts/test_client_logic.mjs` is the acceptance gate for what this entry adds
* **Engineering Notes:**
  * **Files Modified:** `client/index.html`, `scripts/test_client_logic.mjs`, `CLAUDE.md`, `README.md`, `CONTRIBUTING.md`. No server or solver code
  * **Two independent causes, both between a stress value and a pixel.** (1) `COLORMAPS` and `DIVERGING_BWR` hold published sRGB values, but Three r152 defaults `outputColorSpace = 'srgb'` with ColorManagement on, making the shader's working space linear-sRGB — and per Three's contract a vertex-color `BufferAttribute` is assumed to ALREADY be linear and is never converted. The sRGB constants were read as linear and re-encoded on output: a spurious ~1/2.2 brightening applied to the model and not to the legend. (2) `useLegacyLights` defaults true in r152, so intensity is a raw multiplier; ambient 0.45 + directional 1.2 + 0.5 drove a lit facet to ~1.65x its own color (2.15x if a facet could face both directionals). Channels then clipped at 1.0 INDEPENDENTLY, which rotates hue rather than merely brightening
  * **The diagnosis was confirmed numerically before any code changed.** Simulating the pipeline — colormap stop → gamma → vertex attribute → light multiply → sRGB encode — reproduced the screenshot's colors exactly: `#b116bb` magenta at the bottom of the scale where the legend showed `#440154` deep purple, `#82f0eb` pale mint at 25% where the legend showed `#238b8d` teal. **A rendering complaint with no reproduction steps is still a measurable claim; the screenshot was the measurement.** Guessing at "washed out" and reaching for gamma would have gone to the wrong subsystem
  * **Why GAMMA was NOT retuned, having been the obvious suspect.** `GAMMA = 0.55` warps position ALONG the colormap; what was broken was the color rendered AT that position. The two compose only in appearance. Changing it would have been nudging a display constant because output "looks better" — the exact move `CLAUDE.md` warns against for calibration constants — and would have broken the #142 locking test for no defensible reason. It stays at 0.55
  * **Why the meshes are lit at all, rather than unlit.** `MeshBasicMaterial` is the honest choice for a quantitative field: no lighting term, so the color is unconditionally the data. It also erases all 3D form-reading, which on a flat plate like the one in the screenshot leaves an unreadable silhouette. The alternative taken is a rig whose intensities sum to exactly 1.0, with white lights and matte (`specular: 0x000000`) materials: a fully-lit facet renders its colormap color EXACTLY and every other facet a darkened version of the right hue. Shading can still read the part's form; it can no longer misreport a number. This was surfaced as an explicit call before implementing, with the clamped rig as the recommendation, and was not contested
  * **The cost, recorded because it is a real regression in a different axis:** peak illumination drops from ~1.65x to 1.0x, so the whole viewer is darker — bolt markers and force arrows included. Those now render their specified colors instead of blown-out versions, so the direction is correct, but it is a visible change well beyond the heatmap and was flagged as needing human eyes rather than a test
  * **Two latent defects found in the same path, fixed here:** `_colorInteriorValues` kept its own copy of the `disableGamma` URL flag instead of calling `currentGamma()`, so the section cut-face ignored the in-app LINEAR/γ toggle — in the one view where the cut face and the surface are adjacent, and where #190 requires them to agree. And `refreshUnitsDisplay` rewrites every legend label from scratch, which would have dropped the new percentile-clip markers on the first SI/imperial toggle
  * **The p02..p98 clip was silent and is now stated.** With a peak above p98, every vertex from p98 up painted one color while the legend read as if p98 were the maximum — in the reported screenshot, a legend topping out at 16.9 MPa beside a MAX STRESS card reading 24.56 MPa. End labels now carry `≥` / `≤` plus a tooltip giving the real peak
  * **Deferred rather than bundled:** the weld spatial hash matches on cell occupancy with no distance check, chains groups transitively, and is vertex-order dependent. Real, but latent and causally unrelated to the washout — filed as #292 instead of being mixed into a rendering fix where it would have made both halves harder to review and to revert
  * **Two orchestration lessons:**
    1. **A test that compares a subsystem to its own re-derivation cannot see a bug in the layer below both.** Group [L] (issue #142) verified that the legend's CSS bytes matched what the model path *would* compute — but both sides were evaluated with `stressColor`, the sRGB helper, and neither modeled `outputColorSpace` or the lights. It was self-consistent and passed throughout, while the two subsystems it was written to reconcile were emitting visibly different colors. Group [T] closes the gap by modeling the renderer's actual output transform and comparing the GPU's emitted bytes to the legend's, which is the property a user can actually check
    2. **When two subsystems must agree, pin the agreement, not each side's inputs.** #142 aligned the two gamma CURVES and closed; the SPACE mismatch survived because nothing asserted the end-to-end equality. The new gate also parses the light intensities out of `initThree` so retuning the rig without preserving the unit sum fails in CI rather than in a screenshot, and asserts the pre-fix path misses by >40/255 on viridis dark purple so the check cannot quietly decay into a tautology

---

### Entry 15: "Strange Pockets, No Symmetry, No Infill" — Three Rejected Fixes, One Wrong Metric, and a Mesher Sizing Mismatch

* **Date:** 2026-08-10 (branch `claude/stress-symmetry-review-5458x7`, issues #294-#298)
* **Task:** A screenshot of the results view with three complaints: strange pockets of stress in seemingly random spots, less symmetry than expected, and no visible infill. Fully green suite, no reproduction steps. Follow-up instruction reframed it: the solver is held to 1e-12 bit-identity and a 1e-8 CG residual, so if the first thing a viewer looks at is not at that standard the rest is undermined
* **Verification:** `npm run test` exits 0 — 1003 vitest tests across 94 files (976/91 at branch point), 187 solver validation tests, the parallel-assembly equivalence check, 182 client logic checks, and all five drift guards green including the #198 count guard against updated README/CONTRIBUTING numbers
* **Pattern:** Audit → issues → phased plan → small PRs. A measurement-only pass first, five numbered issues (#294-#298), then commits against them in dependency order. No production code was touched until three candidate fixes had been prototyped and rejected on measurement
* **Engineering Notes:**
  * **Files Modified:** `server/gmsh_mesh.ts`, `server/analysis.ts`, `server/solver/symmetry.ts` (new), three new test files, `ROADMAP.md`, `docs/INVARIANTS.md`, `README.md`, `CONTRIBUTING.md`. No change to the solver, the recovery, or any material model
  * **The solve was never the problem, and separating the stages proved it.** On a symmetric cantilever where every bit of output asymmetry is artifact: displacement mirror-asymmetry 0.001-0.012% rms, element-centroid stress 0.14-0.45%, SPR nodal stress 0.52-1.83%. Tip deflection 7.83 mm against Euler-Bernoulli 8.14; peak recovered stress 22.97 MPa at 3k elements against an analytic root bending stress of 23.44. **The displayed field is four transformations downstream of the quantity that carries the 1e-8 guarantee, and differentiation alone costs two orders of magnitude** — which is unavoidable in any FE code and is not a defect
  * **Three candidate fixes prototyped, all rejected on measurement. The negative results are the deliverable.** (1) Boundary-patch borrowing, the Zienkiewicz-Zhu textbook treatment for one-sided surface patches, made it WORSE (surface roughness median 6.31% → 7.02%, asymmetry p95 0.34% → 0.77%): the existing code deliberately centres the basis on the node so the polynomial is never evaluated away from the patch centre, and borrowing a neighbour's patch reintroduces exactly that extrapolation. (2) The recovery cascade was instrumented — only 8 nodes (the box corners) ever fall past the quadratic fit at ANY refinement level, zero reach averaging, and mixed-branch neighbourhoods are LESS rough than uniform ones. (3) `SPR_MAX_AMPLIFICATION_QUADRATIC` swept 60 → 20 → 10 → 5 is bit-identical output; at 3 it degrades. **The recovery is at a local optimum and should not be changed speculatively**
  * **A metric error of my own, caught by distrusting a result that was too clean.** The first surface-vs-interior comparison normalised by GLOBAL PEAK and reported the surface as 7-12x less symmetric than the interior. Interior nodes of a bending plate sit near the neutral axis where stress is low, so peak-normalisation flatters them for free. Re-run with LOCAL normalisation and a 20%-of-peak floor the gap survives (7.6-11.7x) but the absolute numbers collapse to median 0.035-0.068%, p95 0.30-0.74% — **sub-1%, therefore not the visible defect at all.** The original "17% max" was peak-normalised and sat in the clamp band, which is a genuine singularity already modelled by `bcSingularityErrorFraction` (#259). Right smell, wrong culprit, and the correction only came from re-deriving the metric
  * **The decisive experiment was two different meshes, not a finer one.** Same geometry, same load, same element count, different node placement: median disagreement 0.03% of peak, p95 7.90%, max 16.05%. The field is excellent almost everywhere with a scattered artifact tail, and **the hot spots MOVE when the part is re-meshed** — which is exactly the reported symptom. Note p95 |A-B| EXCEEDS p95 |A-ref| (5.84%): two coarse meshes disagree with each other more than either disagrees with the fine reference, the signature of independent random artifact rather than systematic under-resolution
  * **The existing error estimator is structurally blind to this, and that is mechanism not oversight.** Spearman rank correlation between mesh A's own per-element `errorEstimate` and the actual A-vs-B disagreement at the same location: **0.015**. ZZ differences the recovered field against the raw element field; an artifact inherited by BOTH cancels in the difference. `globalRelativeError` remains valid for the energy-norm error it was built for, but `topErrorElements` must not be read as "here is where the picture lies" (#294)
  * **The mesher finding, which is the one that produced code.** `tetMaxVolumeForTier` is scale-relative — bbox volume over `TET_TARGET_ELEMENTS` (4,000/12,000/40,000) — while the STEP path's `clOpts` table was absolute millimetres. **"Fine" meant 40,000 elements for an STL and 2 mm elements for a STEP,** and only the first guarantees a resolution budget. The absolute form is defensible for feature resolution and `clCurv` is what refines bores, so it was kept as an upper bound rather than replaced; `gmshSizingForTier` now takes the finest of three bounds (tier absolute, count budget, through-thickness floor) so it can only refine, with a reported overshoot clamp as the pathology guard. `ROADMAP` had an entry for teaching the STL path to honour the tier and no STEP counterpart, and no landed-decision doc covered the absolute choice — which is what made it a gap rather than a deliberate design
  * **A constant set by convention, then corrected by measurement one commit later.** `MIN_ELEMENTS_THROUGH_THICKNESS` shipped at 3 ("standard FE practice", tagged MEDIUM on that basis). Measuring the two-region sandwich stiffening against resolution: 1 element through thickness recovers 4% of the converged effect, 2 gives 57%, 3 gives 83%, 4 gives 100%. **Three leaves 17% behind; the constant is now 4 and its confidence rests on a STORMFEA measurement rather than on a textbook.** At one element through thickness the two-region model returns essentially the homogenized answer while reporting itself active
  * **The two-region blocker was mis-stated in this same branch, and the correction is recorded rather than quietly edited.** ROADMAP first claimed infill was blocked because the wall-band classification "has nothing to bite on" at current element sizes. That is wrong: against the exact analytic shell volume fraction the classification is within 3.2% with the element 4.4x the band width and 0.06% at h = 1.5 mm, because `tetFractionBelowIso` integrates the level set INSIDE the element (two-region invariant 2). **Volume fraction converges fast; the through-thickness layering is the binding constraint.** Sizing the mesh against the classification would have optimised the wrong quantity
  * **Symmetry detection, and why it cannot verify by matching mesh entities.** The condition being detected is a symmetric part with an ASYMMETRIC mesh, so a test requiring node-to-node correspondence would reject every real case while passing every synthetic one. `detectSymmetryPlanes` measures each mirrored sample point against the surface as a geometric object via `pointTriangleDistance`. There is a test for exactly the failure mode: interior nodes displaced until no mesh correspondence survives, surface untouched, detection must still succeed — and it asserts that premise before asserting the conclusion
  * **A defect in that detector, found by printing the output rather than by reading the test result.** It reported SIX planes for a box, which has three. The covariance is built from the boundary triangulation's centroid cloud and that cloud is chiral, so the principal axes of an axis-aligned box come back rotated by ~1e-4 rad — measured `[0.000225, 0.000460, 1.000000]`, a dot of 1 - 1.3e-7 against the true axis. That passed a 1e-9 duplicate test, then passed verification on its own (residual 3.7e-4 under a 1e-3 acceptance), and was reported as distinct. **The test asserted x/y/z were PRESENT and passed throughout.** Fixed with a 1-degree dedup and coordinate axes ordered first; the replacement test asserts exactly three across four mesh densities, because the rotation only appears once the triangulation is fine enough
  * **Deferred rather than bundled:** the surface-clipping and mirror-weld half of #296 (needs a mesher to validate end to end, and the seam weld has to be exact or it becomes a fresh artifact source), flipping the two-region default (#297, gated on #295 being confirmed against a real part), and an unbounded spatial-grid allocation when `dMax` is far below element scale (#298, unreachable from any current caller)
  * **The honest limit on all of it:** this environment has neither TetGen nor Gmsh. Every number above is from in-repo structured fixtures. The mesher sizing change in particular is NOT confirmed end to end against a real STEP file, which is why `gmshSizingForTier` was written pure and mesher-free — so its logic is testable even where its effect is not
  * **Three orchestration lessons:**
    1. **A negative result on a textbook fix is worth more than an untested improvement.** The Zienkiewicz-Zhu boundary-patch treatment is the standard answer to one-sided surface patches and it degraded every metric here, because the existing implementation had already made a better choice for a documented reason. Prototyping and measuring cost three short experiments; shipping it would have been a regression with a citation attached
    2. **When a measurement flatters the thing you expected to be fine, check the normalisation before believing it.** Peak-normalisation made interior nodes look 10x better than surface nodes purely because they sit near the neutral axis. The gap was real but the magnitude was an artifact of the metric, and the corrected numbers moved the conclusion from "the surface recovery is the defect" to "the surface recovery is sub-1% and the defect is elsewhere"
    3. **A constant justified by convention is a placeholder; measure it before the branch closes.** `MIN_ELEMENTS_THROUGH_THICKNESS = 3` was defensible, documented, and wrong by 17% of the effect it exists to preserve. The measurement that corrected it was three solves and existed only because the NEXT issue needed the same number — nothing in the first commit's own review would have surfaced it

---

### Entry 16: Watertight Surface Clipping — the Cap Was the Hard Part, but the Bug Was in the Epsilon

* **Date:** 2026-08-10 (branch `claude/surface-clipping-nw8hdi`, issue #300)
* **Task:** Open-ended: read CLAUDE.md, read the ROADMAP, pick up the surface-clipping work. #300 had been split out of #296 the previous session precisely because it is the risky half — given a closed triangulated surface and a plane, produce the closed surface of the half-space intersection
* **Verification:** `npm run test` exits 0, with the new `surface-clip.test.ts` contributing 19 tests, and the #198 count guard green against updated README/CONTRIBUTING numbers
* **Pattern:** Single-issue implementation with a mesher-free test surface. The issue text already contained the design and the failure modes, so the work was implementation plus the measurement that the issue could not have predicted
* **Engineering Notes:**
  * **Files Modified:** `server/solver/clip.ts` (new), `server/tests/unit/surface-clip.test.ts` (new), `server/tetgen.ts` (one function delegates instead of repeating a literal), `ROADMAP.md`, `README.md`, `CONTRIBUTING.md`. No change to the solver, the recovery, any material model, or any user-facing path — the clipper is not wired into analysis, which stays under #296
  * **The cap is the feature, and it is not a triangle fan.** Cutting a plate through its bore leaves the bore's cross-section as an INNER loop, so the cap is a constrained triangulation of a polygon with holes. A fan over the outer loop tiles across the bore and hands the mesher a solid where the part has a hole: closed, plausible, and wrong. Outer loops and holes are separated by CONTAINMENT DEPTH rather than by winding — depth 0 is an outer boundary, depth 1 a hole in it, depth 2 an island in that hole — which is robust to a loop whose traversal came out unexpectedly and generalises for free to a cut with several disjoint cross-sections
  * **The bug was not in the algorithm, it was in comparing a float to zero.** With the polygon-with-holes machinery working and the cap area landing one ULP from the analytic value, the fixture still produced **7 zero-area cap triangles and 6 triangulation fallbacks**. Cause: boundary vertices that are collinear in the PART are only collinear to round-off in the DATA. The plate fixture's square edge is built as `t·cosθ` with `t = half-width/|cosθ|`, so it lands on x = 30 ± 1e-14 — and an exact `>= 0` predicate reads a point sitting on an edge as outside it about half the time. The ear clipper therefore cut a diagonal straight along a run of the boundary and left the run behind as a degenerate chain. **The fix is that a 2D cross product is an AREA, so its tolerance scales with diag², not diag.** Both counts go to zero; the cap area was already exact and never moved
  * **Which is the point worth keeping: the area check could not have found this.** Cap area agreed with the analytic square-minus-bore-polygon to 2.6e-16 relative both before and after, because the spurious triangles have zero area and contribute nothing. Only an explicit per-triangle sliver count on the result surfaced it, so `degenerateCapTriangles` and `capFallbackCount` are reported fields rather than internal diagnostics. This is the same shape as the #166 sliver gate and the `-m` background-metric episode in Entry 12: a degenerate element is invisible to every integral you would naturally check it with
  * **Closure is built, then checked, never inferred.** TetGen given an unclosed PLC does not reliably fail — it can tetrahedralise something that is not the part — so a downstream mesh-quality gate is not a substitute. Three construction choices make closure structural: one snap pass on VERTICES before any triangle is classified (so every triangle sharing a vertex agrees about it); split points cached per EDGE, so the two triangles sharing an edge get the same index and bit-identical coordinates; and sign tests only ever read the snapped distances. `checkSurfaceClosure` then verifies every DIRECTED edge is used exactly once, which catches a flipped neighbour that an undirected edge count passes — there is a test for exactly that
  * **A coplanar face is decided by its own normal, not by a special case.** A flat face lying exactly on the plane is an ordinary FTC bracket. The outward normal points away from material and the discard side is the +n side, so a coplanar face bounding kept material has `normal·n > 0`. Getting it backwards either deletes a real face or keeps a wall bounding material that is no longer there, and the closure check catches both — but the rule is one line and needs no enumeration of cases
  * **The shared tolerance was made structural rather than documented.** #296's mirror-and-weld must use the same epsilon as the clip, or the seam does not close: the clip decides which vertices sit exactly ON the plane, the weld decides which of those are the same point afterwards. `weldToleranceForDiag` (`tetgen.ts`) now delegates to `surfaceSnapTolerance` (`clip.ts`) instead of carrying its own `1e-6` literal, and a test asserts the two agree across four scales. A comment saying "keep these in sync" would have been the cheaper option and the one that rots
  * **This environment DID have TetGen, unlike Entry 15's.** `apt-get install tetgen` succeeded once two unrelated third-party PPAs returning 403 were removed from the sources list, so the mesher-gated test was actually exercised rather than left to CI: TetGen accepts the clipped half-plate and tetrahedralises it to the analytic half volume. That test is the one real end-to-end check that a fanned cap would fail — a fan would come back ~235 mm³ heavy on this fixture
  * **Two orchestration lessons:**
    1. **When an issue hands you the design, the value you add is the measurement it could not contain.** #300 correctly named the cap as the hard part, correctly predicted slivers as a failure mode, and correctly listed the tests. What it could not know is that the sliver failure would arrive through a floating-point predicate rather than through the snap logic it was written to defend, and that the natural integral check would be blind to it
    2. **Report the quantity that can disagree with the one you are asserting.** `capArea` versus `loopArea` (the same region computed from the loops alone) is a self-check on the triangulation being a valid tiling, and `degenerateCapTriangles` is a self-check on it being a USABLE one. The first was green throughout while the second was not

---

### Entry 17: The Mesh Tier Promised Two Different Things, and the Path Everyone Called Correct Was Also Broken

* **Date:** 2026-08-10 (branch `claude/issues-roadmap-status-ucuask`, issue #295)
* **Task:** Open-ended, in two halves: first report where the issues and the roadmap actually stand, then act on the answer. The status pass identified #295 as the gating entry of the only dependency chain in NEXT (#295 -> #297 -> #296), and the follow-up instruction was to finish it
* **Verification:** `npm run test` exits 0 — 1038 vitest tests across 96 files (the new `tet-sizing.test.ts` contributing 16), 187 solver validation tests, 182 client logic checks, and all four drift guards green including the #198 count guard against updated README/CONTRIBUTING numbers
* **Pattern:** Single-issue implementation on top of a partially-landed predecessor, where the first real task was working out what "partially" meant
* **Engineering Notes:**
  * **Files Modified:** `server/meshSizing.ts` (new — the shared contract), `server/tests/unit/tet-sizing.test.ts` (new), `server/tetgen.ts`, `server/gmsh_mesh.ts`, `server/analysis.ts`, `client/index.html` (one reliability banner), `docs/mesh-sizing.md` (new), `docs/API.md`, `CLAUDE.md`, `ROADMAP.md`, `README.md`, `CONTRIBUTING.md`
  * **The roadmap's framing of the issue was half right, and the wrong half was the expensive one.** Both #295's text and the ROADMAP entry describe the defect as "the STEP/Gmsh path is absolute millimetres while the STL/TetGen path is scale-relative" — i.e. one broken path and one correct one. PR #299 had already fixed the named path. But a count budget is a budget for the WHOLE part, so a large thin plate spends its elements on plan area and carries almost nothing through the wall. **A 60x30x6 mm plate at the STANDARD tier: 10,800/12,000 = 0.9 mm³ per element, a 1.97 mm regular-tet edge, 3.0 elements through the 6 mm section.** Under the floor, at the DEFAULT tier, on the exact geometry class the tool exists for. The fine tier reaches 4.6 and was always fine, which is probably why it was never noticed. Scale invariance (#168) and a through-thickness floor are independent properties, and having the first says nothing about the second
  * **Which is the transferable part: "this path is already correct" was a claim about a DIFFERENT property than the one under repair.** The STL path genuinely was scale-relative, genuinely had been since #168, and that was genuinely the fix #168 needed. None of that bears on whether a thin section gets enough elements to bend. Two issues had now described the defect in terms that made the STL path sound finished, and the cheapest way to find out was three lines of arithmetic against the tier constants rather than more reading
  * **The floor is expressed in elements-across-a-dimension, which is what makes it scale-free.** TetGen's `-a` takes a VOLUME and Gmsh's `clmax` takes a LENGTH, so both convert through one shared relation (`regularTetVolumeForEdge`, the same 6·√2 as `sizeFieldToVolFile`) rather than two conversions that agree today. The alternative — floors in millimetres per path — would have re-created #168's bug in a new place and passed every test that exists
  * **The prediction is wrong in a direction, and the direction is what makes it usable.** A real mesh emits MORE elements than the regular-tet relation says (the tier path's ratio drifts ~2.5 to ~5.5 with density — what `VOLUME_CAP_SCALE` absorbs on the adaptive path). More elements means SMALLER elements, so a floor derived this way delivers at least the layers it asks for. This is worth stating explicitly in the code rather than apologising for the approximation: an estimator with a known sign is a usable bound, an estimator with an unknown sign is not
  * **The readout had to be measured, not predicted, for a reason specific to these two meshers.** Both treat a size cap as a REQUEST: `meshWithTetGen`'s switch-set fallback chain relaxes `-a` and can end at `-pQ` with no volume constraint at all, and Gmsh's `clmax` yields where a curvature constraint disagrees. A readout built from the flags reports the mesh that was ASKED for — which is exactly the mesh that is not in doubt. `achievedResolution` runs on the returned mesh instead. Same shape as Entry 16's sliver count: report the quantity that can disagree with the one you are asserting
  * **The tests found a bug that review would not have.** `tetMaxVolumeForTier` promised in its own docblock that a degenerate bbox still yields "a usable positive bound", and returned NaN for a non-finite one — `Math.max(NaN, x)` is NaN, so the floor never fired. `meshWithTetGen` catches it again at the call site (`maxVolume > 0` is false for NaN), so nothing shipped broken, but the definition was relying on a guard two modules away to make its own docblock true. It was found by writing the degenerate-input case as a test rather than by reading the function
  * **A test whose premise was wrong, corrected rather than deleted.** The first draft asserted a 50 mm cube with 500 elements would be under BUDGET but fine through THICKNESS. It fails: 500 elements in a cube is 3.9 through the section, so the floor warning fires correctly and the test was simply wrong about the geometry. The fixture moved to 2,000 elements (6.2 through, still 0.05x budget) rather than the assertion moving to match the code
  * **The client banner is a scope call, made against a precedent in this repo.** The issue asks for the shortfall to be "visible rather than silent". The ROADMAP already carries an entry complaining that adaptive refinement is unreachable from the UI because #246 landed the solver path and no readout, so shipping another server-only field would have reproduced a documented mistake. An under-resolved section reads as a part stiffer and stronger than it is — the same optimistic direction as `meshFallback` and the C3D4 caveat — so it uses the existing reliability-banner pattern rather than a new surface
  * **The missing doc was part of the finding, not an afterthought.** #295 cites the absence of any `docs/` writeup as its evidence that the absolute Gmsh sizing was an oversight rather than a landed decision — there was no way to tell the two apart. `docs/mesh-sizing.md` closes that, and records what the change does NOT establish: the floor's measurement was run on the TetGen path, so the STEP path inherits a geometry-derived constant rather than one measured on a STEP part

---

### Entry 18: The Picture Was Correct, Bounded, Non-NaN, Agreed With the Reported Number, and Showed Nothing

* **Date:** 2026-08-10 (branch `claude/issues-roadmap-status-ucuask`, issue #297)
* **Task:** Make the two-region (walls vs infill) model the default and surface the shell/core split, the entry unblocked by #295 the same day. The issue's own scope list: land #295, verify the invariants at the delivered resolutions, make the split visible in the heatmap, change the default
* **Verification:** `npm run test` exits 0 with TetGen installed (so the mesher-gated files actually ran rather than self-skipping), the new `two-region-default.test.ts` contributing 9 tests, and the #198 count guard green against updated README/CONTRIBUTING numbers
* **Pattern:** Single-issue implementation where the issue's stated design was wrong in one specific, discoverable way, and a cheap "would this be useless?" assertion was what discovered it
* **Engineering Notes:**
  * **Files Modified:** `server/analysis.ts` (default flip, resolution gate, nodal classification, volume payload), `server/twoRegion.ts` (unchanged — worth stating, since the model itself needed nothing), `server/meshSizing.ts` (anisotropy caveat), `client/index.html` (default-on toggle, wall/core section-view mode), `server/tests/unit/two-region-default.test.ts` (new), `CLAUDE.md`, `docs/INVARIANTS.md`, `docs/API.md`, `docs/METHODOLOGY.md`, `ROADMAP.md`, `README.md`
  * **The finding: a part's boundary is wall BY CONSTRUCTION, so the classification is constant on the display mesh.** The issue says "make the shell/core split visible in the heatmap," and the obvious reading — publish shell fraction per display-mesh vertex, add a colormap mode — is what the first revision did. Every surface node sits at distance 0 from the surface and therefore inside the wall band, so the field is **identically 1.0 on every part**: measured min 1.0 / max 1.0 on the 24x12x6 fixture against a 50.6% shell volume fraction. The split is an INTERIOR property. It now rides on `volumeField.nodeShellFractionB64` and paints the SECTION CUT, which was the one surface the first revision had explicitly stubbed out as "no data available"
  * **What caught it, and why nothing else would have.** The field was correct, bounded to [0,1], finite, non-NaN, correctly volume-weighted, and consistent with the shell volume fraction the summary reported. Every property worth asserting about a scalar field held. The only failing assertion was `expect(max - min).toBeGreaterThan(0.01)` — "this is not a constant" — written on the theory that a picture which cannot vary is not a picture. **Assert that the output would be USEFUL, not merely that it is correct.** A visualization has a failure mode that a number does not: being right and showing nothing
  * **The gate is the "harden" half, and it comes from a measurement that already existed.** #295's `MIN_ELEMENTS_THROUGH_THICKNESS` was set from a sweep showing that at one element through thickness the two-region model recovers 4% of the converged sandwich stiffening. The consequence had not been drawn: such a run returns the homogenized answer **while reporting itself active**, so the user reads "two-region, shell 51%" on a result that is uniform in all but name. Two-region now degrades with a reason naming the fix. The gate reads `meshResolution` — the mesh that came BACK, not the sizing that was asked for — and an explicit `twoRegion: true` does not override it, because the question is what the mesh can represent
  * **A metric caveat found by using it.** `achievedResolution` reports mean element SIZE across the thin direction, not a layer count. A structured 2x2x0.75 mm mesh through a 6 mm plate has eight nominal layers and reads 3.7, which is why the first test fixture failed the gate it was written to pass. One-sided (anisotropic meshes always read LOW), so a consumer gating on it errs toward declaring a mesh under-resolved — the direction this gate wants. Documented at the definition rather than worked around at the call site
  * **A cost checked rather than assumed.** Flipping the default means `buildTwoRegionField` runs on every analysis, and `materialsEqual` (the 100%-infill collapse) is evaluated only AFTER the distance field is built — so solid parts pay for a field that is discarded. Measured before deciding: 495 ms at 48k elements, 1044 ms at 151k, against solves that run tens of seconds. ~1-2%. Moving the early-out above the distance field would change the reported `shellVolumeFraction` from a true geometric quantity to nothing, which is a worse trade than 1% of wall time, so it was left alone and the number recorded
  * **An adjacent issue checked for reachability instead of being folded in.** #298 (unbounded grid allocation in `computeNodeSurfaceDistancesAndNormals`) is in exactly the code this change makes run on every analysis. It stays latent: both callers derive `dMax` from geometry, so it is always at least one element edge. Worth checking, not worth conflating
  * **The environment was made to match CI rather than trusted to differ from it.** The first full-suite run passed with 8 files self-skipping for lack of a mesher — precisely the files that exercise a default-flip end to end. `apt-get install tetgen` (Entry 16's trick) turned those on locally, which is what made the real-mesher result a fact rather than a hope pinned on CI
  * **Two orchestration lessons:**
    1. **When an issue tells you where to put the output, verify that the output VARIES there.** "Make it visible in the heatmap" was a reasonable instruction that happened to name the one surface on which this particular field is constant. The issue could not have known; the check costs one assertion
    2. **A numbered normative list is an API.** The new invariant was APPENDED as #9, not inserted where it fit thematically, because `docs/INVARIANTS.md` rows and comments across `server/` reference these by number. The first attempt inserted at #5 and silently renumbered four invariants — caught by the numbering collision it also produced, but it would not have been caught by any test

---

### Entry 19: The Mode Named "Contact" Could Not Model a Contact, and the Symptom Everyone Filed Was Three Steps Downstream

* **Date:** 2026-08-11 (branch `claude/open-issues-handoff-5tfx8n`, issue #305)
* **Task:** Open-ended: pick the work up from the handoff the previous session left on the open issues. That handoff was a set of audit comments on #294, #305 and #306; #305's carried an explicit next step, a diagnostic it had costed at "one call, no solve required"
* **Verification:** `npm run test` exits 0 with TetGen installed, 1079 vitest tests across 101 files (the new `contact-patch-surface.test.ts` contributing 11), 187 solver validation tests, the parallel-assembly equivalence check, 182 client logic checks, and all four drift guards green including the #198 count guard against the updated README/CONTRIBUTING numbers
* **Pattern:** Single-issue implementation from a prior session's handoff, where running the handoff's own suggested diagnostic disproved the issue's stated mechanism in the first minute
* **Engineering Notes:**
  * **Files Modified:** `server/solver/load.ts` (the rule), `server/solver/adjacency.ts` (`buildSurfaceTriangleAdjacency`, new), `server/analysis.ts` (ForceSpec docs, the snap warning's wording), `server/tests/unit/contact-patch-surface.test.ts` (new), `server/tests/unit/contact-patch-position.test.ts`, `docs/load-distribution-default.md`, `docs/API.md`, `README.md`, `CONTRIBUTING.md`
  * **The issue named a symptom; the handoff named a mechanism; both were downstream of the actual defect.** #305 was filed as "contact_patch breaks mirror symmetry", with two candidate mechanisms (a rim tie-break, or an order-dependent weight sum). The previous session's diagnostic cleared the second and confirmed a non-symmetric SELECTION, then flagged the number that reframed it: **1 triangle**, where a 2.75 mm disc on a 1.5 mm mesh should cover several. Its proposed next step was to check whether that under-selection was specific to the load point (a corner) or general. It is neither: **`contact_patch` was structurally incapable of loading the surface a push is applied to.** Eligibility was `n·d > 0` — the surface a force points OUT of. A contact pushes, so at the surface it presses on `n·d < 0`, and the only eligible triangles were on the far side of the part, outside the radius. The taper selected nothing, and the "patch fell between the triangles" fallback put the entire 120 N on one triangle of the opposite face, chosen by index among ties
  * **The transferable part: the cheap diagnostic was cheap because it printed a quantity nobody had asked about.** The previous session's dump was aimed at pairing and weights, which is what the issue asked for. `triangles: 1` was incidental output, and it is what made the diagnosis possible — the pairing failure was a consequence, not a cause. When instrumenting a suspected selection bug, print WHAT WAS SELECTED, not only whether the selection has the property under suspicion
  * **The docstring argued for the opposite of the code it documented, and had done since #271.** It justified the windward test as stopping a patch that would "push on a surface it cannot bear against" — reasoning that is only correct for `n·d < 0`. The word "windward" says the same thing: a windward face looks INTO the wind. Two readers had signed off on a comment that contradicted its own line of code
  * **The fix is adjacency, and the reason is that every alternative needs a tolerance.** The patch grows from the triangle nearest the application point by EDGE ADJACENCY while inside the radius. The competing designs — same-side-as-the-anchor normals, or a signed plane offset — need a tolerance that must simultaneously admit a bore's curvature (`r²/2R` of sag across the patch) and reject a thin plate's far face one millimetre away. There is no such number: the default radius is 10% of the bbox diagonal, which on a 60x30x3 plate is 6.7 mm against a 3 mm thickness. Adjacency needs no tolerance, and it answers the thin-plate and wrap-the-rim cases with the same rule
  * **It also removes the tie-break that the symmetry symptom actually came from.** A connected component does not depend on traversal order, so nothing about the patch depends on triangle numbering. The one place an index tie survived — the under-resolved fallback — now loads every TIED nearest triangle. Measured: SPR nodal von Mises mirror asymmetry 5.0441% → 0.0000%, σ₁ 6.4493% → 0.0000%, σ₃ 2.5654% → 0.0000%
  * **The benchmark fixture got 24% more conservative and 4x less mesh-stable, and that is in the doc rather than in a footnote.** The tube's application point is exactly on its outer top rim, so the patch now wraps onto the top annulus, which the windward test had excluded (`n·d = 0` there). SF 1.75 → 1.33 at 20 291 elements, and the spread across three rungs 2.3% → 15.7% against #271's headline "26.6% → 3.6%". **Whether that is the rule or the fixture was worth one more measurement:** the same load moved off the rim to mid-height on the wall holds 4.6% across the same 4x element range. The instability belongs to load points placed on a sharp edge, not to the patch rule — and that distinction only exists because it was measured instead of asserted
  * **A second, smaller alias fixed on the way, for the same reason as the two-region distance field.** `centreSnapMm` — the number `analysis.ts` warns on — was a point-to-CENTROID distance, so a point sitting exactly on a coarse face reported itself 2.24 mm off the surface against a 2.75 mm radius. It is now `pointTriangleDistance`, the helper the two-region model already uses for exactly this reason (CLAUDE.md, two-region invariant 4), and reports 0.000 mm
  * **What was deliberately NOT changed:** the legacy modes still select from `direction` and ignore `position` (#271's split), the taper is still Euclidean rather than geodesic, and the default radius is still an uncalibrated judgement — now doing more of the work, since a wrong-face point load did not depend on it at all

---

### Entry 20: The Spatial Hash Had Stopped Being an Accelerator and Become the Answer

* **Date:** 2026-08-11 (branch `claude/issue-292-p1fjox`, issue #292)
* **Task:** Single-issue implementation: the vertex weld in `computeSmoothedStressColors` grouped by which grid cell was OCCUPIED rather than by distance, so it welded past the documented tolerance, chained groups transitively, and depended on vertex order
* **Verification:** `npm run test` exits 0 — 1079 vitest tests across 101 files (1024 passed, 55 self-skipped along with 9 whole files because TetGen and Gmsh are absent in this container), 187 solver validation tests, the parallel-assembly equivalence check, 202 client logic checks, and all four drift guards green including the #198 count guard against the updated README/CONTRIBUTING numbers
* **Pattern:** Single-issue implementation from a well-diagnosed bug report — the issue had already read the loop correctly and named all three consequences, so the work was the fix, the proof it bites, and the measurement of what it costs
* **Engineering Notes:**
  * **Files Modified:** `client/index.html` (`weldCoincidentVertices`, new; `computeSmoothedStressColors` now calls it), `scripts/test_client_logic.mjs` (test group [U], new), `CLAUDE.md`, `README.md`, `CONTRIBUTING.md`
  * **The defect in one line: the accelerator had become the decision.** A spatial hash exists to shortlist candidates that a real predicate then accepts or rejects. This loop had no predicate. It took the group of the first occupied cell in the 27-cell neighborhood, and — the part that turns a bounded error into an unbounded one — wrote that BORROWED group back under its own cell key, so a run of adjacent occupied cells propagated one group arbitrarily far. Two vertices 0.069 mm apart (~6.9x `WELD_EPS`) merged; forty vertices spanning 0.585 mm merged; and `break outer` on a fixed di/dj/dk scan meant the partition followed vertex index rather than geometry
  * **The fix is the missing predicate, not a smaller cell.** The map now holds, per cell, the ids of the groups whose REPRESENTATIVE lives there; a vertex joins the nearest representative within `WELD_EPS`, or starts its own group registered under its OWN cell. Every member is then within `WELD_EPS` of its rep, which bounds a group at `2*WELD_EPS` and makes chaining structurally impossible. Shrinking the cell instead would have reduced the error's magnitude while leaving all three mechanisms intact
  * **Scanning all 27 cells instead of breaking on the first is what removes the order dependence** — nearest-rep with ties to the lowest group id is a function of the representative set alone. The partition is provably order-independent when clusters have diameter <= `WELD_EPS` and are separated by more than it, which is exactly the STL-duplicate case the weld exists for; test group [U] asserts it over 25 shuffles rather than resting on the argument
  * **The obvious cost did not materialise, and the measurement is why the claim is in the commit rather than a hedge.** Scanning all candidates rather than breaking early should be slower. First implementation was, 1.6x. Visiting the vertex's own cell FIRST and stopping at distance 0 — safe because no two representatives can be coincident, so nothing can beat an exact duplicate — made it 2.5x FASTER than the code it replaces: **482 ms vs 1188 ms on a 405k-vertex display mesh**, for an identical partition on that mesh. That last clause is the evidence for the issue's own "this is latent" framing
  * **The old algorithm lives in the test file, not in git history.** Group [U] carries a `weldByOccupancy` reimplementation purely so each property test can show the pre-fix code fails it: 1 group vs 2 on the neighborhood-corner pair, 1 vs 40 on the spaced run, and a widest-from-rep beyond 5x tolerance on a sliver-scale cloud. Without it the tests restate the implementation; with it they are a constraint. Same technique as group [T]'s pre-fix color-space guard
  * **Why the debug pass was not the fix.** `?debugWeld=true` already computed the exact property that was being violated and would have printed it — but only under a flag nobody sets, so it observed the invariant without enforcing it. It stays as the runtime check on real geometry the synthetic fixtures do not have, and it is now an exact re-derivation: the first vertex of a group in index order IS the representative, because a group is created by the vertex that becomes one
  * **What was deliberately NOT changed:** `WELD_EPS` stays 0.01 mm (the bug was never the tolerance's value), `docs/INVARIANTS.md` gets no row because that file is scoped to the two normative CLAUDE.md invariant lists and a heatmap row would break its stated scope, and the display-mesh weld remains a rep-based clustering rather than a canonical quantization — quantization trades order dependence for cell-boundary dependence, which is the failure the 27-cell search exists to avoid

---

### Entry 21: The Study Had the Spatial Information and Threw It Away

* **Date:** 2026-08-11 (branch `claude/issue-294-fl7rft`, issue #294)
* **Task:** Single-issue implementation: the DISPLAYED stress field carries a mesh-dependent artifact tail that the ZZ estimator cannot flag. The issue's own follow-up comment named the first step — the figures were taken before #295 changed what a mesh tier promises, and nobody had re-measured
* **Verification:** `npm ci && npm run test` exits 0 — 1079 vitest tests across 101 files (1024 passed, 55 self-skipped along with 9 whole files because TetGen and Gmsh are absent in this container), 187 solver validation tests, the parallel-assembly equivalence check, 245 client logic checks, and all five drift guards green including the #198 count guard against the updated README/CONTRIBUTING numbers
* **Pattern:** Measure first, then fix what the measurement licenses. The issue had ruled out three recovery-side fixes on measurement and identified refinement as the only lever; re-measuring said the lever had already been pulled, which changed the deliverable from "reduce the artifact" to "disclose where it is"
* **Engineering Notes:**
  * **Files Modified:** `server/tests/measure294.ts` (new, offline measurement — not in CI), `client/index.html` (`meshSensitivityField` + `installMeshSensitivity` + `MODE_META.meshsens`, new; `legendValueText`, extracted; `convDatumFromPayload`, `runConvergenceStudy`, `refreshUnitsDisplay`, `handleStressHover`, `applyStressColorsFromArrays`, the C3D10 badge copy), `scripts/test_client_logic.mjs` (groups [V] and [W], new), `docs/display-field-mesh-sensitivity.md` (new), `CLAUDE.md`, `ROADMAP.md`, `README.md`, `CONTRIBUTING.md`. Follow-ups filed rather than absorbed: #312 (the 10px divergence) and #313 (the residue of #294)
  * **Re-measuring changed the deliverable.** #294 reported p95 7.90% / max 16.05% of peak at 3,072 elements. At the post-#295 tiers, on the same fixture with the same probes: p95 **1.45%** / max 1.83% at standard and p95 2.15% / max 14.61% at coarse (15% node displacement). Refinement was the lever, #295 pulled it, and the tail shrank monotonically in median, p95 and max. What did NOT move is the mechanism — Spearman between `errorEstimate` and actual mesh-to-mesh disagreement is 0.061 / -0.066 / -0.164, against 0.015 originally. So the remaining defect is not "the field is wrong" but "nothing tells you where it is mesh-dependent"
  * **The measurement's own cap is a finding, not a nuisance.** Mesh B is mesh A with nodes displaced within their own feature (interior free, face in-plane, edge along-edge, corners pinned) so geometry and element count are identical. At 25% of cell size the FINE tier stops being measurable: both seeds tried put an element under the solver's 0.02 normalized-Jacobian floor and the mesh-quality gate refused the solve. That is the right cap to respect — a perturbation that makes a sliver makes a mesh STORMFEA would never show anyone — and it is why the headline series is at 15%. Recorded rather than quietly retuned, because 36,000 elements simply get more chances to draw a sliver than 4,032 do, which is a property of the perturbation model and not of the tier
  * **The fix costs one array subtraction because of a property nobody had used.** Every analyse response paints the SAME display mesh: `server/analysis.ts` maps its nodal field onto `req.positions` — the client's upload-time geometry — whatever the analysis mesh density. So index v in the standard mesh's `vertexStress` is the same point in space as index v in the fine mesh's, and "how much did this location move between meshes" is `|a[v] - b[v]|`, exact and uninterpolated. The convergence study was already solving both and reducing each to a p99. The change is to keep the array
  * **Why not fix the recovery instead.** #294 prototyped and rejected three (boundary-patch borrowing — worse, it reintroduces exactly the off-centre extrapolation the design avoids; the cascade thresholds — only 8 nodes ever leave the quadratic branch; `SPR_MAX_AMPLIFICATION_QUADRATIC` — bit-identical from 60 down to 5). Nothing since has touched SPR. And smoothing the display is barred by CLAUDE.md's rendering invariants: the model's colors ARE the reading. Disclosure was what was left
  * **`null` means unmeasured, and that distinction is load-bearing.** One mesh cannot measure its own mesh-dependence, so with a single solve the mode does not appear at all rather than painting zeros — the same rule `headlineSpread` follows (#256). `installMeshSensitivity` clears the previous field BEFORE any early return, so a declined install (wrong display-mesh size, one usable mesh) can never leave the previous pair's colors on screen under the new run's label. Test group [V] pins that, plus symmetry under argument swap, and that a single moved vertex shows at full amplitude in `max` while leaving median and p95 alone
  * **The C3D10 badge was asserting a measurement it had not taken.** "Mesh-independent within 5% tolerance" sat on a path that returns BEFORE any second mesh is solved. It now states the element property that IS known and points at the study for the rest. This is the same defect class as the silent p02..p98 legend clip: a true-sounding sentence covering an untaken measurement
  * **An adjacent bug fell out of adding a unitless mode.** The legend's label text existed twice and the copies had drifted in opposite directions: the paint path printed unconverted MPa under an imperial LABEL (24.4 "psi"), while the unit-toggle repaint ran `U.stress()` over EVERY mode, so switching units relabelled η, the utilizations and wall/core as stress. A percentage-of-peak legend reading "210.30 psi" is what surfaced it. One `legendValueText` now serves the paint path, the repaint and the hover readout, with `MODE_META.stressUnit` marking the six modes whose numbers are a convertible stress; group [W] pins both directions
  * **The first revision of the UI copied its neighbours instead of `DESIGN.md`, and that was the wrong call.** The convergence panel is built at 10px with 3px radii; both are off the stated scale (four sizes, three radii), so a compliant block renders visibly different from every sibling. The first pass took the local consistency and filed the divergence as #312. But the repo had already answered this: PR #302 shipped a compliant 13px/4px mesh-resolution banner beside five non-compliant 12px/6px ones, and #303 records that as deliberate — "the new code complies and the sweep is left to this issue rather than smuggled into a mesh-sizing PR". Reworked to match: 11px Outfit copy, DM Mono 9px for the data line (the meshes and their percentages are DATA, so they take the mono role), a border-left callout instead of a boxed card, and a `.btn` with no font-size or padding override. Test group [V] pins it scoped to the new blocks, because a whole-file assertion would just fail — the point is that new markup cannot quietly copy a neighbour, not that the file is clean
  * **Confidence is split, because the two claims here do not carry the same weight.** The mechanism (eta cannot rank these locations) is HIGH: it is structural — eta is `‖σ* − σ_h‖` and an artifact in both terms subtracts out — and it reproduced four times across two perturbation amplitudes and three densities. The AMPLITUDES are MEDIUM: one geometry class, one material, structured meshes rather than TetGen output. What they support is the comparison BETWEEN tiers, which is a within-fixture comparison and is what the conclusion rests on; what they do not support is quoting 1.45% as a number for some other part. Erring direction is known and stated: a perturbed structured mesh is a TAMER model of "a different mesh" than a fresh TetGen run, which changes connectivity too, so real spread is likely larger
  * **What was deliberately NOT changed:** SPR recovery (still at its measured local optimum), the artifact itself (refinement remains the only lever on amplitude), `globalRelativeError`'s placement (still RESULTS tab + the η explainer — now tracked as #313 rather than only as a ROADMAP line), the 10px/3px drift in the surrounding panel (that is #312's sweep, and doing it here would bury a measurement PR under a restyle), and the overlay does NOT separate random artifact from ordinary under-resolution — doing so needs two meshes at the SAME density, which doubles the study's cost for a distinction that does not change what the user should do about a flagged spot

### Entry 22: A Latent Crash, and a Coverage Note That Was Wrong About Why It Existed

* **Date:** 2026-08-11 (branch `claude/issue-298-end-to-end-9e5lx3`, issue #298)
* **Task:** Single-issue implementation: `computeNodeSurfaceDistancesAndNormals` sized its triangle-bucket grid from `dMax` with no ceiling, so a `dMax` far below element scale spanned every triangle across millions of cells and the bucket `Map` threw `RangeError: Map maximum size exceeded`
* **Verification:** `npm run test` exits 0 — 1090 vitest tests across 102 files (1035 passed, 55 self-skipped along with 9 whole files because TetGen and Gmsh are absent in this container), 187 solver validation tests, the parallel-assembly equivalence check, 245 client logic checks, and all five drift guards green. The client-logic figure is post-merge with #294, which landed on main mid-branch and took entry 21
* **Pattern:** Single-issue implementation from a well-diagnosed report. The issue had already located the line, proved the failure was unreachable from production, and proposed the fix; the work was implementing it, sizing the budget on measurement, and — unplanned — checking the coverage claim the issue had been filed in service of
* **Engineering Notes:**
  * **Files Modified:** `server/solver/distance.ts` (`chooseGridCellSize` and `buildTriangleBucketGrid`, both new), `server/tests/unit/distance-grid-budget.test.ts` (new), `server/tests/unit/distance-boundary-seed.test.ts`, `docs/INVARIANTS.md`, `ROADMAP.md`, `README.md`, `CONTRIBUTING.md`
  * **Reproduced before fixing, because "latent" is a claim about reachability, not about whether the bug is real.** 34 s of grid thrash then the RangeError, on the issue's own fixture — a 20x10x5 box with 56 boundary triangles at `dMax = 1e-6`. Afterwards: 5 ms
  * **The fix is a budget, not a rejection, and the reason is what makes it safe.** The grid is an acceleration structure; every distance comes from the point-triangle kernel over whatever candidates a query collects. Raising the cell only widens that set, and `cell >= dMax` is what keeps the 27-cell one-ring complete — so no returned number can change. That argument is only worth as much as its test, so the locking assertion is that the gridded output equals a NO-GRID brute-force reference exactly (`toBe`, not `closeTo`) at every cell size tried, coarsened and not
  * **The issue named one function; the hazard was in two.** `computeNodeBandPenetration` carried a verbatim copy of the same 40-line grid. Fixing only the reported symbol would have left an identical crash one call away, so the grid was extracted into a single builder and both now share it
  * **The first budget was correct and still wrong-sized.** A flat 2M-insertion cap makes the function total, which is what the issue asked for — but it let a 56-triangle mesh spend 518 ms building a grid finer than its own geometry. The budget that matters is per-triangle: cells finer than a triangle buy no selectivity, they just copy the same triangle into more buckets. 64 cells per triangle against the ~8 a legitimate `dMax` needs took the reported case to 5 ms. The flat cap stayed as a backstop for absurd triangle counts
  * **Half the test file pins a NEGATIVE, which is the half that would have rotted silently.** If the budget were ever tightened, every real analysis would coarsen — slower, still correct, and invisible to every other assertion in the suite, because coarsening changes no output. So `chooseGridCellSize` is exported purely so tests can assert `cell === dMax` on production-shaped inputs, plus an eighth of that `dMax` to show the margin is not a coincidence of one fixture. Verified as a constraint by tightening the budget to 4 and to 1 and watching them fail
  * **The coverage note this issue was filed in service of was wrong, and only checking it found that.** #298 came out of `distance-boundary-seed.test.ts`, whose note said the crash blocked the test that would prove the SEED loop rather than the point-triangle sweep zeroes boundary corners, and that the curved-bore case proved it instead — "corners come back bit-exactly 0 while swept midsides land at ~1.8e-15, therefore different code paths." The conclusion does not follow. A boundary corner is a VERTEX of a boundary triangle, so it sits at distance 0, inside ANY positive radius however small, and Ericson's kernel returns `p − a` in the vertex region — bit-exact 0, not noise. The sweep produces exactly what the seed does
  * **Settled by deleting the seed loop and running the file: every test in it still passed.** So the seed is an optimization (boundary nodes skip the sweep), not a semantic, and no test on a well-formed mesh can separate them. Invariant #4's "boundary nodes seed at exactly 0" holds on both paths; the tests pin the VALUE, which is what `wallfrac` consumes. `INVARIANTS.md` row 4 and the file's note now say that instead of claiming a discrimination that does not exist
  * **What was deliberately NOT done:** no rejection path or thrown error for a small `dMax` (it is a legitimate search radius, just an expensive one — the docblocks now state the expected magnitude instead), no change to either production caller's `dMax` derivation, and the issue was not closed on the strength of the fix being written. The two functions' behavior at the `dMax` production passes is unchanged, which is the point

---

### Entry 23: The Only Asymmetric Thing Left Was the Mesh, and Two Confounders Nearly Said Otherwise

* **Date:** 2026-08-10 (branch `claude/issues-roadmap-status-ucuask`, issue #296, PR #307 — the continuation of Entry 18's session). **Backfilled on 2026-08-12** from the git history during a documentation audit: this work landed on `main` without an entry, which the audit found as a gap in this log rather than in the work
* **Task:** Finish #296, the last step of the dependency chain Entry 18's status pass identified (#295 → #297 → #296). Detection had landed in `822afb6` (Entry 15) and the clipper in #300/#301 (Entry 16); what remained was the mirror itself, the acceptance measurement the issue body specified, and wiring the five stages into `runAnalysis`
* **Verification:** from the commits and the tree: 19 tests across the three #296 files at wiring time (10 in `mirror-mesh.test.ts`), `tsc` clean, and `cefdf2b` updating the guarded README/CONTRIBUTING counts to 1,068 tests across 100 files for the #198 drift guard
* **Pattern:** Invariant-locked refactoring, with the acceptance criterion fixed in the issue body BEFORE the implementation — the measurement was the deliverable and the code was what made it possible to take
* **Engineering Notes:**
  * **Files Modified:** `server/solver/mirrorMesh.ts` (new, `mirrorTetMesh`), `server/tests/unit/mirror-mesh.test.ts` (new, 10 tests), `server/tests/unit/mirror-symmetry-acceptance.test.ts` (new), `server/analysis.ts` (the `symmetryMesh` block in `runAnalysis`, `SymmetryMeshReport`), `server/tests/unit/symmetry-mesh-pipeline.test.ts` (new), `README.md`, `CONTRIBUTING.md`. Now documented in `docs/METHODOLOGY.md` §3.4 and `docs/ARCHITECTURE.md` §4.2
  * **The acceptance measurement is the entry, and two confounders nearly inverted it.** The first attempt read 4.87% against 5.04% — "mirroring does nothing". Both readings were artifacts. (1) With `holes: []` the bar had no constraint, so the solve ran away on a rigid-body mode to ~1e16 mm and both fields were noise; every measurement now asserts convergence and a sane deflection as a premise. (2) The DEFAULT load distribution is not symmetric: `contact_patch` produced 5.04% SPR asymmetry on a PERFECTLY symmetric mirrored mesh with the load placed exactly on the symmetry plane, where `uniform` produced 0.0000%. A symmetry measurement taken with the default load was measuring the load. **Filed separately rather than worked around, and that filing became #305** — where the 5.04% turned out to be a single-triangle fallback on the wrong face of the part (Entry 19)
  * **The premise is asserted before the conclusion.** The baseline's chirality is measured first: 128 of 384 element centroids pair, the same 33.3% the issue reports for its own fixture. An earlier revision of the file skipped that and would have compared two symmetric meshes and concluded nothing. Result: chiral 33.3% paired / 3.909% SPR asymmetry, mirrored 100% / 0.0000%, with principal stresses checked as well as von Mises since von Mises alone can hide a sign-flipped asymmetry
  * **Not fixing the orientation was the decision, not an oversight.** A reflection reverses element orientation. The assemblers auto-orient through `Math.abs(sixV)` / `Math.abs(detJ)` and the mesh-quality gate keys on scale-invariant shape metrics rather than the Jacobian SIGN — deliberately, so a mirror-oriented but well-shaped mesh solves and passes. Permuting corners to restore a positive sign would also require permuting the six C3D10 midsides to match `C3D10_EDGE_PAIRS`: the same index surgery that produced the #66 and #265 midnode defects. Copying each element's node list verbatim through the index map keeps every edge relationship intact by construction
  * **The seam is SNAPPED, not merely matched, and the epsilon is shared rather than duplicated.** A node 1e-12 off the plane mirrors to a DISTINCT node 2e-12 away, producing near-coincident pairs and slivers between them — the same defect class as the vertex-welding bug in CLAUDE.md's heatmap section. Nodes within tolerance are projected ONTO the plane first, so a seam node's image is bit-identical to itself and maps back to its own index. The tolerance is `surfaceSnapTolerance` (`clip.ts`), the same definition the clip uses to decide what lies on the plane, made structural in Entry 16 precisely so the seam cannot fail to close
  * **A straddling input throws instead of silently overlapping.** `NotAFundamentalDomainError`, because the alternative is a mesh laid over itself: a part twice as stiff, which no mesh-quality gate would flag
  * **Opt-in, with the same staging as the two-region model.** It changes the mesh of every symmetric STL part and costs an extra mesh — detection needs a mesh to run on, so the part is meshed once to detect and once more for the half. Land opt-in, measure, then flip. End to end on a 24x12x6 bar through TetGen at the coarse tier: plain mesh **0.07%** of element centroids have a mirror partner, symmetry mesh **99.85%** (half 7,130 elements → mirrored 14,260, seam 1,417 shared nodes), both solving and agreeing on peak displacement within 25%
  * **Every failure path keeps the mesh already in hand, so this can degrade but never break a solve.** No plane detected, a clip that discards everything or does not close, a degenerate cap, a TetGen failure on the half, a straddling input — all fall back to the ordinary mesh and report the reason on `summary.symmetryMesh`. Degradation is a REPORT, not a warning: most parts are not symmetric and that is not a problem
  * **A fixture bug worth recording, because the symptom did not name the cause.** The test soup was wound INWARD (signed volume −1728). It passes the manifold check — closed, zero inconsistent edges — but makes the clip emit its cap on the wrong side, which surfaces as inconsistent edges in the OUTPUT. Safe (the closure check catches it and the run degrades) and filed separately, since real STLs ship with inverted normals
  * **What was deliberately NOT changed:** the default (opt-in until measured on real parts), element orientation after reflection (above), `surfaceToNode` on this path becomes an identity map — TetGen's `-Y` STL-vertex correspondence cannot survive clipping and nothing consumes the map, since every display mapping is a spatial nearest-node lookup — and the claim was scoped honestly at the time: mirroring does NOT by itself give a symmetric picture on a force-loaded part, which is why both #296 test files pin `uniform` and say why

---

### Entry 24: Five Banners Had the Wrong Numbers Because There Were Six Copies of Them

* **Date:** 2026-08-12 (branch `claude/resolve-issue-303-kj4p0z`, issue #303). Written live
* **Task:** Fix #303 — the results reliability banners carried a 12px headline and a 6px card radius, and `DESIGN.md` permits neither (type scale 9/11/13/16, radius scale 0/2/4px). Five of the six were wrong; the sixth, added by PR #302 for #295, complied and so rendered one step larger than the five beside it
* **Verification:** `npm run test` green end to end before the commit — 1,035 vitest tests passed with 55 environment-skipped across 102 files, 187 solver validation tests, 256 client logic checks, and all five drift guards including the #198 count guard
* **Pattern:** Closest to invariant-locked refactoring, but the honest version is that **the invariant did not exist yet**. `DESIGN.md` stated the rule and nothing enforced it, so the lock is as much the deliverable as the sweep. The two-region and interlayer models get this treatment by default; the frontend has had no equivalent, which is why a documented rule drifted five times without anything going red
* **Engineering Notes:**
  * **Files Modified:** `client/index.html` (new `reliabilityBannerHTML`, six call sites rewritten in `showResults`), `scripts/test_client_logic.mjs` (test group [X], 11 checks), `CLAUDE.md` (Frontend Design System), `README.md` and `CONTRIBUTING.md` (client-check count 245 → 256 for the #198 guard)
  * **The numbers were the symptom; the copies were the defect.** Editing five font sizes and five radii would have closed the issue and left the mechanism running — the issue body says as much ("Six literal copies is what let the drift happen"), and its own history proves it, since the pattern reached five instances by being copied. The markup now has ONE definition. Adding a seventh banner is a call, not a copy, which is what makes the next one compliant by construction rather than by review
  * **Sizes are named as tokens, not px literals.** `var(--text-base)` / `var(--text-sm)` / `var(--text-xs)`, per `DESIGN.md`'s review checklist ("no px literal"). This is not cosmetic: a literal is correct only at the instant it is written, and the file already carries 40 token references against several hundred literals. Test group [X] therefore asserts the TOKENS still hold their documented values as well as asserting the helper names them — a token reference is only compliant while the token is
  * **The body is interpolated as HTML, deliberately, and the test pins that.** Escaping it would have been a defensible-looking "improvement" and a behaviour change: the mesh-fallback copy carries a `<b>`, and three server-supplied strings (`unitsWarning`, `rigidBodyMode.message`, `meshResolution.warning`) were already trusted this way at all six sites. Changing trust boundaries inside a type-scale fix would have buried a security-relevant decision in a design commit
  * **The anti-drift half of the test is the half that matters.** Pinning the helper's output only proves the helper is right; it says nothing about a seventh banner written by hand next to it. So group [X] also extracts the `reliabilityBanner` append chain and asserts no `font-size:` or `border-radius:` survives anywhere in it, and that the count of appends equals the count of helper calls. Verified to be a real constraint rather than a tautology by reverting the headline to 12px and confirming the group goes red
  * **Two findings filed rather than folded in, because both are different rules from the one #303 names.** #319: 12px/14px and the 6px radius still exist outside the banners (17 sites), and the pointed case is the verdict card rendered directly BELOW the banner stack, still at 6px — so this fix removes the mismatch #303 describes and exposes a different one until that lands. #320: the banner background is a hardcoded `#3a1a00` while every other colour in the block is a token; it fails contrast in light theme (body copy 2.56:1) and in the print/PDF export (1.33:1, black on near-black, because `@media print` remaps the text tokens but the inline background is not `!important`-overridable from `body`). The print case is the one worth acting on — the exported PDF is what a user hands to someone else, and these banners are exactly the content that must survive that trip
  * **The contrast ratios were computed, not eyeballed.** WCAG 2.x relative luminance from the token hex values in the file, not sampled from a screenshot — stated that way in #320 so a future session knows what kind of evidence it is
  * **10px was deliberately not touched.** It is #312's open question (whether the four-size scale or the code is the thing that is wrong), and #303's second comment argues #312 should settle first or this sweep gets redone. That ordering does not bind here: 12px is forbidden under EITHER resolution of #312, and the prescribed banner treatment uses 13/11/9 and no 10px, so the fix is independent of the decision. Waiting would have been deference to a dependency that is not one
  * **What was deliberately NOT changed:** the `12px 14px` padding and the 4px headline gap (14px and 4px are off the four-value spacing scale, but `DESIGN.md`'s own card component rule specifies `10px 14px`, so the scale and the component rules disagree and that is not #303's question); the `#3a1a00` background (#320); the same literal on the "Known combination gaps" block in `renderValidationCoverage`, noted in #320 as same-pass work
  * **A `CLAUDE.md` accuracy note, since that file's own rules ask for one.** The first draft of the new bullet quoted "268 occurrences" of 10px. That number is measured-now and drifts with every unrelated edit — #312 recorded 270 for the same grep two days earlier — so it was replaced with "the file's most-used size by a wide margin", which is the claim that stays true. Same reason that file already says to cite symbols rather than line numbers

---

### Entry 25: Flagging an Edge-Spanning Patch Instead of Reshaping It

* **Date:** 2026-08-12 (branch `claude/issue-308-resolution-hr8bhd`, issue #308). Written live
* **Task:** Fix #308 — a `contact_patch` load placed exactly on a sharp geometric edge wraps onto both faces the edge joins (correct), but its peak stress does not converge with mesh refinement the way a flat-face patch does (15.7% spread vs 4.6% over the same 4x element range on the Ø5-bore tube fixture). The issue named three candidates and left the choice open: a geodesic taper (the only one that restores the spread), flagging the condition on the existing reliability machinery (cheapest, no anchor moved), or documenting this as expected corner physics and closing without a code change
* **Verification:** `npm run test` green end to end before the commit — 1,095 vitest tests across 103 files, 187 solver validation tests, 256 client logic checks, all five drift guards passing
* **Pattern:** Closest to invariant-locked refactoring in spirit (a documented geometric threshold with a locking test) but the underlying issue was itself a three-way design choice, not a defect with one correct fix — so the entry records which option was taken and why the other two were set aside, not just what changed
* **Engineering Notes:**
  * **Files Modified:** `server/solver/load.ts` (`assembleContactPatchLoad` now returns `maxLoadedNormalAngleDeg`; new `CONTACT_PATCH_SHARP_EDGE_DEG` constant), `server/analysis.ts` (`loadPatchSharpEdgeWarning` on `AnalysisResult`, assembled in the `contact_patch` force loop), `client/index.html` (seventh reliability banner), `scripts/test_client_logic.mjs` (banner-count assertion 6 → 7), `server/tests/unit/contact-patch-sharp-edge.test.ts` (new), `docs/load-distribution-default.md`, `CLAUDE.md` (light-shard file count), `README.md`/`CONTRIBUTING.md` (#198 count guard)
  * **Option 2 chosen over Option 1 for the reason the issue itself gave.** A geodesic taper is the only fix that would actually restore the 3.6% mesh spread #271 shipped, but it changes edge weights on every CURVED surface, not just sharp ones — Euclidean-vs-geodesic distance diverges continuously with curvature, so a Dijkstra-over-centroids taper needs its own before/after measurement across the whole curved-surface population, which is out of scope for a confidence disclosure. Flagging moves no validated anchor and does not change a single reported number.
  * **Why this could not reuse the existing `load-edge` singularity cause.** `detectSingularity`'s rim test measures distance from the peak to the loaded patch's OUTER boundary (`loadRimPoints`, from `bcDiscontinuityMask`). #308's peak sits 0.4-0.7mm from the application point, inside a 1.769mm-radius patch — near its CENTRE, not its rim — so the existing signal is not reliably positioned to catch it. The new signal is computed at load-assembly time instead, from the patch geometry itself, independent of where the stress peak lands.
  * **The signal is a normal-angle check between ADJACENT LOADED triangles, reusing machinery already in the function.** `assembleContactPatchLoad` already builds `buildSurfaceTriangleAdjacency` for the growth BFS and already computes each triangle's normal (previously only its magnitude, for area) — hoisting the adjacency build out of the `if` branch and caching the unit normal made the scan an O(loaded triangles) pass with no new traversal. It runs on the tied-fallback path too (radius smaller than the local mesh), since that path selects triangles by distance rather than by this adjacency but can still straddle an edge.
  * **45 degrees, not 90, and stated as a geometric threshold rather than a physical constant.** The issue's own example is a 90 degree rim; 45 sits below that with margin while staying well above the few-degree normal drift between neighbouring triangles on an ordinarily-meshed curved surface, so a smooth cylinder does not trip the flag. No literature citation applies — it is tuned by, and locked by, the new test file, per CLAUDE.md's confidence-tagging rule for non-physical thresholds.
  * **The test file pins both the geometry and the non-firing case.** A patch entirely on one flat face (any radius, including one wide enough to reach four-plus triangles) reads under 1 degree; a patch centred on a cube's 90 degree edge reads ~90 degrees, past the threshold; the single-triangle tied-fallback patch reads exactly 0 (no adjacent loaded pair to measure). The "stays ~0 as the patch grows on a flat face" case exists specifically to rule out a false positive on an ordinary wide load.
  * **Wired through the same shape as `unitsWarning`, not a new mechanism.** One `string | null` field on `AnalysisResult`, newline-joined across forces, rendered as one more `reliabilityBannerHTML` call — chosen so it participates in #303's anti-drift guard (`scripts/test_client_logic.mjs` group [X], which counts helper calls in the banner chain and fails if a banner is hand-rolled) rather than inventing a parallel disclosure path outside it.
  * **What was deliberately NOT done:** no change to the patch's weights or to any validated safety-factor number (the fix is purely additive disclosure); no change to `server/report.ts` (the PDF report already omits `unitsWarning` and `meshResolution.warning` — this follows that existing precedent rather than being the first field to widen the PDF's reliability surface); the peak-vs-neighborhood singularity heuristic (`detectSingularity`) was left untouched rather than taught to also search inside a patch, since that would change its behavior on every load, not just edge-spanning ones.

---

### Entry 26: The Sweep #303 Deferred

* **Date:** 2026-08-12 (branch `claude/issue-319-resolution-8jmkca`, issue #319). Written live
* **Task:** Fix #319 — the 12px/14px font-sizes and the 6px card radius still present in `client/index.html` outside the six (now seven, after #308) reliability banners #303 fixed. 17 sites total: 5 `font-size:12px`, 3 `font-size:14px`, 9 `border-radius:6px`. #319 explicitly scoped these three values as the first, judgement-call pass and left the 8/18/22/32/36px sizes and 1/3/5/8/20px radii for later, pending the splash-screen exception classification and #312's resolution of whether 10px belongs on the scale
* **Verification:** `npm run test` green end to end after the change — 1040 vitest tests passed with 55 environment-skipped across 103 files, 187 solver validation tests, 256 client logic checks, and all drift guards (invariant symbols, client identifiers, nullish precedence, doc/test counts, API routes) green
* **Pattern:** Closest to a straight sweep, but each site was a genuine judgement call rather than a mechanical find-and-replace, per the issue's own framing ("each a judgement call about role")
* **Engineering Notes:**
  * **Files Modified:** `client/index.html` only — 17 inline `style` attributes, each one property changed (`font-size` or `border-radius`), nothing else on the line touched
  * **Radius was the easy half.** DESIGN.md's radius scale ties `4px` to "Cards, inputs, dropdowns, popup panels" and every one of the 9 sites (the onboarding coachmark popup, the five `actionHTML` verdict cards in `showResults`, the demo-archetype picker modal, the demo-mode callout chrome, and the keyboard-shortcut overlay panel) is exactly that kind of container — so all nine became `4px` with no ambiguity. This closes the issue's most pointed example verbatim: the verdict card immediately below the reliability banner stack now shares its radius instead of stepping to 6px.
  * **Font-size was decided by matching each site to a sibling that was already compliant, not by picking a table role in isolation.** The `SETTINGS` panel header (14px) was set to 13px to match the already-compliant `NATURAL FREQUENCIES` / `BUCKLING` section headers elsewhere in the file — same role, same font (Rajdhani 700), same letter-spacing pattern. The error-panel "friendly fix" line (12px) was set to 11px specifically because it sits directly below a 13px headline in the exact same two-tier headline/body shape `reliabilityBannerHTML` already uses at 13/11 — the precedent this issue's own sibling fix established was reused rather than re-derived. The welcome-modal description paragraph (12px) went to 13px (`--text-base`, "body, descriptions") since it is exactly that: a description, distinct in role from the 11px callout note directly beneath it in the same modal, which was already compliant and untouched.
  * **Two sites had no visible text to anchor a decision to.** The help-button `?` glyph (a circular 30px button, `textContent = '?'`) and the demo-mode close button's `font-size` (a button containing only an `<svg>` icon, so the property does not affect anything rendered) were both sized by nearest sibling rather than by role, since neither has a paragraph-level "what is this" to match: 13px for the `?` (nearest allowed value to the old 14px, and large enough to read in a 30px circle) and 11px for the inert icon-button property (matching the sibling text sizes in the same demo-mode overlay, so the property stays harmless rather than becoming a second inconsistency).
  * **The count in the issue body was the acceptance test.** `grep -c "font-size:12px"`, `font-size:14px"`, and `border-radius:6px"` all read 0 after the change, confirming the 5+3+9=17 sites are the entire set and nothing was missed or double-counted.
  * **What was deliberately NOT done:** no change to the 5px onboarding button radius (a documented separate defect per the issue's own radius table, second pass); no change to any 8/18/22/32/36px font-size or 1/3/5/8/20px radius site (explicitly out of scope, pending the splash-screen exception classification); no `var(--text-*)` token conversion — most of the surrounding file still uses px literals for `font-size` (only `reliabilityBannerHTML` and a handful of newer call sites use tokens), so converting these 17 sites to tokens in isolation would have been a bigger, unrequested refactor rather than the scoped sweep the issue asked for; no grep-style CI guard added (the issue names that as the durable fix but says explicitly it "can only land after the sweeps, and after #312 decides what the scale actually is" — #312 is still open).

---

### Entry 27: The Banner Background Was the One Property #303 Didn't Tokenize

* **Date:** 2026-08-12 (branch `claude/issue-320-resolution-pupqv3`, issue #320). Written live
* **Task:** Fix #320 — `reliabilityBannerHTML` and the "Known combination gaps" block in `renderValidationCoverage` both painted `background:#3a1a00` as an inline hex literal while every other color in the same markup (`--warn`, `--text-mid`, `--text-lo`, `--bg-base`) was already a token. #303 moved this markup into one helper but only fixed the type-scale and radius duplication; the hex literal moved with it untouched, because #303 was scoped to layout, not color. Per the issue's own WCAG relative-luminance measurements, this made the banner ~2.56:1 (body copy) and ~4.14:1 (headline) in light theme (`[data-theme="light"]` redefines `--text-mid`/`--warn` but not a hardcoded hex), and ~1.33:1 in print, where the `@media print` block forces the rest of the page to a white/black palette but has no lever on a literal on a descendant element
* **Verification:** `node scripts/test_client_logic.mjs` green after the change — 260 client logic checks including 5 new assertions this fix adds. The full `npm run test` (vitest + solver + doc/API/invariant drift guards) was not run in this session's sandbox — no `node_modules` installed — but the change touches only `client/index.html` CSS custom properties and two inline `background:` values, nothing in `server/` or the TypeScript build
* **Pattern:** Closest to invariant-locked refactoring in spirit (one property, three per-theme values, locked by a new test assertion) but the underlying choice — reuse the existing `--warn-faint` token vs. add a new dedicated one — was a real design decision, resolved by asking rather than assuming
* **Engineering Notes:**
  * **Files Modified:** `client/index.html` (new `--warn-banner-bg` token in the dark `:root`/`[data-theme="dark"]` block, the `[data-theme="light"]` block, and the `@media print` `[data-theme="dark"]` override; both `background:#3a1a00` call sites swapped to `background:var(--warn-banner-bg)`), `scripts/test_client_logic.mjs` (5 new assertions in test group [X]), `CLAUDE.md` (the stale "tracked in #320, not fixed by #303" note updated to describe the fix)
  * **`--warn-faint` already existed (`#D6862E18`) but was rejected as the swap target.** It is defined only in the dark-theme block (no light-theme value at all — using it as-is in light theme would resolve to nothing), and more importantly, substituting it would visibly lighten the dark theme's banner from a near-black box to a translucent amber tint over the card background — a real, user-visible change to a surface nobody asked to have re-designed. Asked the user directly rather than picking either option unilaterally; the answer was a new dedicated token that keeps dark theme pixel-identical.
  * **No single background color satisfies AA 4.5:1 for both existing text tokens simultaneously, and that is a property of the tokens, not of this fix.** Computed from the relative-luminance formula: `--text-mid` (light theme, L≈0.132) can only reach 4.5:1 against a background with luminance ≥0.768 (a light background); `--warn` (light theme, L≈0.225) cannot reach 4.5:1 against any background with luminance ≤1 — its ceiling is ~3.82:1 at pure white, since its own luminance sits too close to mid-range for a wide ratio in either direction. Verified precisely for the chosen values: `--warn-banner-bg:#FBF4EA` (light theme) gives `--text-mid` 5.65:1 (passes) and `--warn` 3.5:1 (below 4.5, at the token's own ceiling); print's `#fff` combined with print's already-darker `--warn:#9a5512` remap gives 21:1 and 5.7:1 (both pass, since print's `--warn` override — pre-existing, not touched by this fix — has enough headroom that pure white clears it). The residual light-theme headline gap is documented in CLAUDE.md as a pre-existing limitation of the shared `--warn` token (already used the same way elsewhere in the app), not something this fix introduced or was scoped to fix.
  * **The dark-theme value is the old literal verbatim (`#3a1a00`), not re-derived.** This was the explicit condition attached to the "new token" choice — a computed or "improved" dark-theme value would have been an uninvited visual change exactly like the `--warn-faint` swap it was chosen to avoid.
  * **The "Known combination gaps" block got the identical fix in the same pass**, per the issue's own framing ("same literal ... same reason ... worth doing in the same pass") — same token, same three per-theme values, no separate design decision needed since it shares `--warn` and the same background role.
  * **What was deliberately NOT done:** no change to `--warn` or `--text-mid` themselves (would ripple into every other consumer of those tokens across the file); no attempt to force full AA on the light-theme headline by darkening the background past what still reads as a distinct card from `--bg-card` (white in light theme) — the border and warning-triangle icon already carry that visual signal; no new locking test for the `comboHTML` block specifically (it had none before this fix either), though the new group [X] assertions do cover it since the fix and the token are shared.

---

### Entry 28: Wiring a Solver-Only Flag to the UI, and Deciding Not to Flip It

* **Date:** 2026-08-17 (branch `claude/issue-309-rmblex`, issue #309). Written live
* **Task:** Fix #309 — symmetry-preserving meshing (#296) shipped in #307 as `analysis.symmetryMesh`, consumed by `runAnalysis` but never reachable over HTTP (absent from `ANALYSE_SPEC`, the analyse handler's settings object, and `buildPayload`), so no client could turn it on or read `summary.symmetryMesh`. Two halves: wire it end to end, AND decide whether to flip the default on, which the issue left open with three options (opt-in + control; default on with silent degrade; default on under a mesh-cost threshold)
* **Verification:** `npx tsc --noEmit` clean; `npm run test`'s vitest portion green in this sandbox — 1040 passed / 55 environment-skipped across 103 files (TetGen/Gmsh absent, so `symmetry-mesh-pipeline.test.ts` self-skips as designed); `node scripts/test_client_logic.mjs` green at 266 checks (6 new in group [Y]); the API-route and invariant-symbol drift guards both green. The full serial `npm run test` (including the solver shard and the doc-count merge) was not run — that shard needs the TetGen binary CI installs
* **Pattern:** Human-in-the-loop design decision (the default was a genuine product choice with a known asymmetric cost, so it was put to the user rather than assumed) wrapped around straight cross-layer wiring
* **Engineering Notes:**
  * **Files Modified:** `server/index.ts` (`symmetryMesh` in `ANALYSE_SPEC`, forwarded into the settings object with strict `=== true`, and added to `buildPayload`'s summary), `client/index.html` (opt-in toggle in the ANALYSIS panel; request field in `buildAnalyseBody`; a degraded-case reliability banner; an applied-case info line on the mesh-stats row), `scripts/test_client_logic.mjs` (new group [Y]; banner count 7 → 8), `docs/API.md` (moved `symmetryMesh` out of both the "not reachable over HTTP" note and the dropped-payload table into the request/response tables), `ROADMAP.md` (#309 resolution under the #296 entry), `README.md`/`CONTRIBUTING.md` (#198 client-logic count 260 → 266)
  * **The decision was OPT-IN, and it was the user's to make, not mine.** "Default on" (either variant) doubles mesh time on every symmetric part — detection needs a mesh to run on, so the part is meshed once to detect and once for the half — for a benefit only parts with a symmetry plane ever see. The two "default on" options trade that cost against a better default picture; the threshold variant adds gate logic to bound the worst case. None is obviously correct, so it went to `AskUserQuestion` rather than a guess. The answer was opt-in: keep the cost where the user asked for it. This mirrors, but deliberately did NOT copy, the two-region model's "default on" staging (#297) — two-region's extra cost is a classification pass on a mesh already built, not a whole second mesh, so the cost profiles are not comparable and the defaults justifiably differ.
  * **Good news and bad news take different surfaces.** The reliability banners are amber-warning chrome for "do not trust this result at face value," so routing a successful mirror ("your mesh was mirrored") through one would misuse the semantics. Split instead: `applied === true` reads as a neutral fact on the existing mesh-stats line (`N nodes · M elements · symmetry-preserving (mirrored about plane, K seam nodes)`); `applied === false` — the user opted in and it silently did not take, so the picture may still be lopsided — is the actual caveat and gets the banner. Null (never opted in) surfaces nothing on either path; both branches short-circuit on a falsy `s.symmetryMesh`. Group [Y] pins all three branches so a later edit cannot collapse them.
  * **The banner rides #303's anti-drift machinery rather than a parallel path.** It is one more `reliabilityBannerHTML` call in the `showResults` chain, so it participates in group [X]'s "every banner goes through the helper, none is hand-rolled" guard — which is why the count assertion there moved 7 → 8 in the same change. Wiring it as bespoke markup would have dodged that guard.
  * **This resolves exactly one row of a documented multi-field defect, and says so.** `docs/API.md` already listed `symmetryMesh` among the `AnalysisResult` fields `buildPayload` computes and drops (alongside `meshResolution`, `meshOrderDowngrade`, `bucklingResult`, and others whose client UI is consequently inert). Rather than silently deleting the row, the doc now records `symmetryMesh` as the first field lifted out of that list by wiring, with the rest still pending — so the defect stays visible and the precedent for fixing the others is on record.
  * **What was deliberately NOT done:** the default was not flipped (the decision above); the sibling dropped fields (`meshResolution`'s and `loadPatchSharpEdgeWarning`'s never-fired banners, `bucklingResult`, etc.) were NOT wired in this pass — each is its own scope with its own UI, and widening #309 to a `buildPayload` audit would have buried the one change asked for; no session save/restore entry was added for the new toggle, matching the existing two-region/CLT/wall-bond toggles, none of which are persisted either; no server-side HTTP integration test was added (the repo has no such harness, and the same-shaped fields it would test have none) — the report contract the client reads (`applied`/`reason`/`seamNodeCount`/null) is already locked by `symmetry-mesh-pipeline.test.ts`, and the client-side wiring by group [Y].

---

### Entry Template
* **Date:** [YYYY-MM-DD, or a range plus the PR/issue numbers for a campaign]
* **Task:** [Briefly describe what you worked on]
* **Verification:** [What is verifiable about the work: CI state, suite counts, locking tests]
* **Pattern:** [Which orchestration pattern — see the list at the top. Optional for one-off entries]
* **Engineering Notes:** [What files were modified, and what engineering choices were made. For anything non-obvious, record WHY the rejected alternative was rejected — that is what a future session with no memory of this work actually needs]
